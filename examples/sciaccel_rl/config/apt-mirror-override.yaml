# Use the internal apt mirror during builds because external traffic times out.
# Task runtime stays isolated from the network.
services:
  main:
    build:
      args:
        APT_MIRROR: "http://mirrors.tencentyun.com"
