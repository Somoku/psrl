# Miniconda installer binary (pre-downloaded by build_image.sh, not committed).
Miniconda3-latest-Linux-x86_64.sh
# Pre-packed mlgym_generic conda env tarball (regenerated by build_image.sh).
mlgym_generic.tar.gz
