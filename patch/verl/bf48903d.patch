diff --git a/verl/models/transformers/qwen3_5.py b/verl/models/transformers/qwen3_5.py
index 3217ac5e..7b63d633 100644
--- a/verl/models/transformers/qwen3_5.py
+++ b/verl/models/transformers/qwen3_5.py
@@ -587,6 +587,12 @@ def forward_with_torch_backend(
     vocab_weights = self.lm_head.weight
     if isinstance(vocab_weights, DTensor):
         vocab_weights = vocab_weights.full_tensor()
+    # `full_tensor()` keeps the local shard's device. With CPU offload, or while TMS has reclaimed
+    # the trainer's GPU memory, that is the host, while `hidden_states` stay on GPU and the fused
+    # matmul then fails with "mat2 is on cpu, different from other tensors on cuda". Moving the
+    # lm_head is cheaper than moving the activations.
+    if vocab_weights.device != hidden_states.device:
+        vocab_weights = vocab_weights.to(hidden_states.device)
 
     ulysses_sequence_parallel_size = get_ulysses_sequence_parallel_world_size()
     if shift_labels is None and ulysses_sequence_parallel_size > 1:
@@ -646,6 +652,10 @@ def forward_with_triton_backend(
     hidden_states = hidden_states.to(vocab_weights.dtype)
     if isinstance(vocab_weights, DTensor):
         vocab_weights = vocab_weights.full_tensor()
+    # See `forward_with_torch_backend` above: `full_tensor()` does not pin the device, and an
+    # offloaded lm_head must be moved to the activations' device before the fused matmul.
+    if vocab_weights.device != hidden_states.device:
+        vocab_weights = vocab_weights.to(hidden_states.device)
 
     log_probs, entropy = linear_cross_entropy(
         hidden_states,
diff --git a/verl/protocol.py b/verl/protocol.py
index 20483011..baef38d9 100644
--- a/verl/protocol.py
+++ b/verl/protocol.py
@@ -936,22 +936,32 @@ class DataProto:
         # Merge meta_info with special handling for metrics
         merged_meta_info = {}
         if data:
-            # Merge non-metric meta_info and aggregate metrics from all workers.
-            all_metrics = []
+            # Collect all keys from all workers
+            all_keys = set()
             for d in data:
-                for k, v in d.meta_info.items():
-                    if k == "metrics":
+                all_keys.update(d.meta_info.keys())
+
+            # Merge non-metric meta_info and aggregate metrics from all workers.
+            all_metrics = []  # for key "metrics" only
+            for k in all_keys:
+                if k == "metrics":
+                    for d in data:
+                        v = d.meta_info.get(k)
                         if v is not None:
-                            if isinstance(v, list):
-                                all_metrics.extend(v)
-                            else:
-                                all_metrics.append(v)
-                    else:
-                        if k in merged_meta_info:
-                            # Ensure consistency for overlapping non-metric keys
-                            assert merged_meta_info[k] == v, f"Conflicting values for meta_info key '{k}'"
-                        else:
-                            merged_meta_info[k] = v
+                             if isinstance(v, list):
+                                 all_metrics.extend(v)
+                             else:
+                                 all_metrics.append(v)
+                elif k.endswith("_metrics"):
+                    # For xxx_metrics: merge as np.array([dict1, dict2, ...]) from each worker
+                    values = [d.meta_info.get(k) for d in data]
+                    merged_meta_info[k] = np.array(values)
+                else:
+                    # Ensure consistency for overlapping non-metric keys
+                    vals = [d.meta_info[k] for d in data if k in d.meta_info]
+                    if vals:
+                        assert all(vals[0] == v for v in vals), f"Conflicting values for meta_info key '{k}'"
+                        merged_meta_info[k] = vals[0]
 
             # Flatten list of dicts to dict of lists for consistent metrics structure
             if all_metrics:
diff --git a/verl/single_controller/ray/base.py b/verl/single_controller/ray/base.py
index bb6a6043..d014cd5a 100644
--- a/verl/single_controller/ray/base.py
+++ b/verl/single_controller/ray/base.py
@@ -119,6 +119,7 @@ class RayResourcePool(ResourcePool):
         max_colocate_count: int = 10,
         detached=False,
         accelerator_type: Optional[str] = None,
+        resource_num_per_bundle: Optional[float] = 1.0,
     ) -> None:
         super().__init__(process_on_nodes, max_colocate_count)
         self.use_gpu = use_gpu
@@ -127,6 +128,7 @@ class RayResourcePool(ResourcePool):
         self.pgs = None
         self.detached = detached
         self.accelerator_type = accelerator_type
+        self.resource_num_per_bundle = resource_num_per_bundle
 
     def get_placement_groups(self, strategy="STRICT_PACK", name=None, device_name="cuda"):
         if self.pgs is not None:
@@ -145,7 +147,7 @@ class RayResourcePool(ResourcePool):
 
         bundle = {"CPU": self.max_colocate_count}
         if self.use_gpu:
-            bundle[device_name] = 1
+            bundle[device_name] = self.resource_num_per_bundle
             if self.accelerator_type is not None:
                 bundle[self.accelerator_type] = 1e-4
         pg_scheme = [[bundle.copy() for _ in range(process_count)] for process_count in self._store]
@@ -626,7 +628,7 @@ class RayWorkerGroup(WorkerGroup):
         if self.use_gpu and not use_gpu:
             raise ValueError("use_gpu is True but resource_pool.use_gpu is False")
         local_world_size = resource_pool.store[0]
-        num_gpus = 1 / resource_pool.max_colocate_count
+        num_gpus = resource_pool.resource_num_per_bundle / resource_pool.max_colocate_count
 
         # we pass in environment variable at option so that Worker can use environment variable to set
         env_vars = {
diff --git a/verl/trainer/config/data/legacy_data.yaml b/verl/trainer/config/data/legacy_data.yaml
index 2971c6b5..8f0ad7dd 100644
--- a/verl/trainer/config/data/legacy_data.yaml
+++ b/verl/trainer/config/data/legacy_data.yaml
@@ -65,7 +65,7 @@ return_raw_chat: True
 return_full_prompt: False
 
 # Whether to shuffle the data in the dataloader.
-shuffle: True
+shuffle: False
 
 # Seed to use when shuffling the data
 seed: null
diff --git a/verl/trainer/config/engine/fsdp.yaml b/verl/trainer/config/engine/fsdp.yaml
index 201acac7..6566c040 100644
--- a/verl/trainer/config/engine/fsdp.yaml
+++ b/verl/trainer/config/engine/fsdp.yaml
@@ -38,6 +38,8 @@ use_orig_params: false
 # Random seed for reproducibility.
 seed: 42
 
+load_weight: true
+
 # Whether to enable full determinism for distributed training, only for debugging.
 full_determinism: false
 
diff --git a/verl/trainer/config/engine/megatron.yaml b/verl/trainer/config/engine/megatron.yaml
index f1e2ba40..b4bd26df 100644
--- a/verl/trainer/config/engine/megatron.yaml
+++ b/verl/trainer/config/engine/megatron.yaml
@@ -66,6 +66,10 @@ entropy_from_logits_chunk_size: 2048
 # oc.select: default val for ref.megatron.seed
 seed: 42
 
+load_weight: true
+
+use_per_rank_checkpoint: False
+
 # Allow to override Distributed Data Parallel (DDP) config
 override_ddp_config: {}
 
diff --git a/verl/trainer/config/model/hf_model.yaml b/verl/trainer/config/model/hf_model.yaml
index bef40f1d..3ebc6856 100644
--- a/verl/trainer/config/model/hf_model.yaml
+++ b/verl/trainer/config/model/hf_model.yaml
@@ -16,7 +16,7 @@ hf_config_path: null
 tokenizer_path: null
 
 # whether to use shared memory for model loading
-use_shm: False
+use_shm: True
 
 # whether to trust remote code.
 trust_remote_code: False
diff --git a/verl/trainer/ppo/core_algos.py b/verl/trainer/ppo/core_algos.py
index a89bc392..05c3e192 100644
--- a/verl/trainer/ppo/core_algos.py
+++ b/verl/trainer/ppo/core_algos.py
@@ -1145,6 +1145,7 @@ def agg_loss(
     batch_num_tokens: Optional[int] = None,
     global_batch_size: Optional[int] = None,
     loss_scale_factor: Optional[int] = None,
+    session_loss_weights: torch.Tensor | None = None,
 ):
     """
     Aggregate the loss across global batch to ensure the loss is invariant to fsdp/megatron parallelism.
@@ -1160,6 +1161,10 @@ def agg_loss(
         dp_size: data parallel size
         batch_num_tokens: number of valid tokens in global batch
         global_batch_size: global batch size
+        session_loss_weights: Per-row reciprocal of (valid session count * session token count),
+            computed over the full training window before DP/micro-batch splitting. Shape (bs,).
+            Empty sessions receive zero weight. This mode does not use `batch_num_tokens`
+            or `global_batch_size`; splitting a session across rows must not change its weight.
         loss_scale_factor: scale factor for "seq-mean-token-sum-norm" mode. If None, uses loss_mask.shape[-1].
             Set this to a constant value to ensure consistent normalization throughout training.
 
@@ -1167,7 +1172,15 @@ def agg_loss(
         loss: `a scalar torch.Tensor`
             aggregated loss
     """
-    if loss_agg_mode == "token-mean":
+    if loss_agg_mode == "session-mean-token-mean":
+        if session_loss_weights is None:
+            raise ValueError("session-mean-token-mean requires session_loss_weights from the full training window.")
+        if session_loss_weights.shape != loss_mat.shape[:1]:
+            raise ValueError("session_loss_weights must contain exactly one weight per loss row.")
+        # FP32 session weights keep the masked reduction stable for low precision losses.
+        session_mask = loss_mask * session_loss_weights.unsqueeze(-1)
+        loss = verl_F.masked_sum(loss_mat, session_mask) * dp_size
+    elif loss_agg_mode == "token-mean":
         if batch_num_tokens is None:
             if dp_size > 1:
                 raise ValueError("(global) batch_num_tokens is required when dp_size > 1")
diff --git a/verl/trainer/ppo/metric_utils.py b/verl/trainer/ppo/metric_utils.py
index c9c8c1e9..6546a120 100644
--- a/verl/trainer/ppo/metric_utils.py
+++ b/verl/trainer/ppo/metric_utils.py
@@ -456,8 +456,8 @@ def compute_data_metrics(batch: DataProto, use_critic: bool = True) -> dict[str,
     advantages = batch.batch["advantages"]
     returns = batch.batch["returns"]
 
-    max_prompt_length = batch.batch["prompts"].shape[-1]
-    max_response_length = batch.batch["responses"].shape[-1]
+    max_prompt_length = batch.meta_info.get("max_prompt_length", batch.batch["prompts"].shape[-1])
+    max_response_length = batch.meta_info.get("max_response_length", batch.batch["responses"].shape[-1])
 
     response_mask = batch.batch["response_mask"].bool()
 
@@ -471,6 +471,26 @@ def compute_data_metrics(batch: DataProto, use_critic: bool = True) -> dict[str,
     non_aborted_sequence_score = sequence_score[non_aborted_mask]
     non_aborted_sequence_reward = sequence_reward[non_aborted_mask]
 
+    if "original_reward_score" in batch.non_tensor_batch and "data_source" in batch.non_tensor_batch:
+        np_non_aborted_mask = non_aborted_mask.numpy()
+        non_aborted_original_scores = batch.non_tensor_batch["original_reward_score"][np_non_aborted_mask]
+        non_aborted_data_sources = batch.non_tensor_batch["data_source"][np_non_aborted_mask]
+        data_sources, data_source_counts = np.unique(non_aborted_data_sources, return_counts=True)
+        print("In this batch, the data sources are:")
+        for data_source, data_source_count in zip(data_sources, data_source_counts):
+            print(f"{data_source}: {data_source_count}")
+        print("--------------------------------")
+        data_src2original_score_metrics = defaultdict(dict)
+        for data_source in data_sources:
+            ds_mask = non_aborted_data_sources == data_source
+            ds_original_scores = non_aborted_original_scores[ds_mask]
+            data_src2original_score_metrics[data_source]["mean"] = np.mean(ds_original_scores)
+            data_src2original_score_metrics[data_source]["max"] = np.max(ds_original_scores)
+            data_src2original_score_metrics[data_source]["min"] = np.min(ds_original_scores)
+    else:
+        data_src2original_score_metrics = {}
+        data_sources = []
+
     if non_aborted_sequence_score.numel() > 0:
         score_mean = torch.mean(non_aborted_sequence_score).detach().item()
         score_max = torch.max(non_aborted_sequence_score).detach().item()
@@ -552,6 +572,15 @@ def compute_data_metrics(batch: DataProto, use_critic: bool = True) -> dict[str,
         critic_value_metrics = {}
 
     metrics = {
+        **{
+            key: value
+            for data_source in data_sources
+            for key, value in {
+                f"critic/original_score/{data_source}/mean": data_src2original_score_metrics[data_source]["mean"],
+                f"critic/original_score/{data_source}/max": data_src2original_score_metrics[data_source]["max"],
+                f"critic/original_score/{data_source}/min": data_src2original_score_metrics[data_source]["min"],
+            }.items()
+        },
         # score
         "critic/score/mean": score_mean,
         "critic/score/max": score_max,
@@ -978,8 +1007,9 @@ def process_validation_metrics(
             var_dict = uid_dict.setdefault(uid, {})
 
             for var_name, var_vals in var2vals.items():
-                # skip empty or string values
-                if not var_vals or isinstance(var_vals[0], str):
+                # skip empty, string, or non-numeric values (e.g. dicts from
+                # reward_extra_info that aggregate per-loop reward metadata)
+                if not var_vals or not isinstance(var_vals[0], (int, float)):
                     continue
 
                 # compute mean and std
diff --git a/verl/trainer/ppo/padding_utils.py b/verl/trainer/ppo/padding_utils.py
index 436e17bc..59e102f1 100644
--- a/verl/trainer/ppo/padding_utils.py
+++ b/verl/trainer/ppo/padding_utils.py
@@ -23,7 +23,6 @@ from __future__ import annotations
 
 import copy
 import logging
-import uuid
 from typing import Any
 
 import torch
@@ -71,13 +70,18 @@ def construct_minimal_padding_template(
     source_td: dict,
     source_tag: dict,
     eos_token_id: int,
+    pad_uid: int,
 ) -> tuple[dict, dict]:
     """Construct a minimal text-only padding template of one prompt token and one response token.
 
+    Fields copied from the source that carry one entry per response token are shrunk to a single
+    token, so the synthetic row stays consistent with the minimal response built below.
+
     Args:
         source_td: A single sample dict retrieved from TransferQueue.
         source_tag: The corresponding tag dict for that sample.
         eos_token_id: The EOS token id from the tokenizer.
+        pad_uid: Integer uid assigned to the synthetic sample.
 
     Returns:
         A tuple of (template_sample, template_tag) ready for padding.
@@ -88,6 +92,14 @@ def construct_minimal_padding_template(
         value = source_td[key]
         template_sample[key] = value.clone() if isinstance(value, torch.Tensor) else copy.deepcopy(value)
 
+    # A response-token field copied from the source is longer than the one-token response built
+    # below, and `to_padded_tensor` pads each field to its own max. Shrink by length, not by name.
+    source_response_len = source_td["response_mask"].shape[0]
+    if source_response_len > 1:
+        for key, value in list(template_sample.items()):
+            if isinstance(value, torch.Tensor) and value.dim() == 1 and value.shape[0] == source_response_len:
+                template_sample[key] = torch.zeros(1, dtype=value.dtype, device=value.device)
+
     # Deep copy the template tag from an existing sample.
     template_tag = copy.deepcopy(source_tag)
 
@@ -101,6 +113,7 @@ def construct_minimal_padding_template(
 
     # Update the fields and remove redundant parts
     template_sample.update(
+        uid=pad_uid,
         prompts=prompts,
         responses=prompts.clone(),
         input_ids=input_ids,
@@ -112,6 +125,9 @@ def construct_minimal_padding_template(
         rm_scores=torch.zeros_like(response_mask, dtype=torch.float32),
         rollout_log_probs=torch.zeros_like(response_mask, dtype=torch.float32),
     )
+
+    if "parent_id" in template_sample:
+        template_sample["parent_id"] = pad_uid
     if "multi_modal_inputs" in template_sample:
         template_sample["multi_modal_inputs"] = {}
     if routed_experts is not None:
@@ -120,7 +136,7 @@ def construct_minimal_padding_template(
         template_sample.pop("routed_experts", None)
 
     # Padding flag is deployed to protect metrics calculation (e.g. response length, score, reward).
-    template_tag.update(is_padding=True, prompt_len=1, response_len=1, seq_len=2)
+    template_tag.update(is_padding=True, prompt_len=1, response_len=1, seq_len=2, parent_id=pad_uid)
     return template_sample, template_tag
 
 
@@ -154,26 +170,18 @@ def upsample_batch_to_divisible_size(
     source_key = batch.keys[source_idx]
     source_td = tq.kv_batch_get(keys=[source_key], partition_id=batch.partition_id)[0]
 
-    # Construct the minimal padding template of one prompt token and one response token
-    template_sample, template_tag = construct_minimal_padding_template(source_td, batch.tags[source_idx], eos_token_id)
-
-    # All padding data use the same uid (also the same trajectory_id 0 but with ascending session_ids)
-    # This uid is not identical to any of the actual data, so it won't affect the grpo advantage value.
-    pad_uid = f"pad{uuid.uuid4().hex}"
-    template_sample["uid"] = pad_uid
-
     # Construct the padding samples in a for-loop
     pad_keys = []
     pad_tags = []
     pad_fields = []
     pad_size = batch_multiple - remainder
     for local_idx in range(pad_size):
-        sample = copy.deepcopy(template_sample)
-        # Use incremental local_idx as different session_ids
-        pad_keys.append(f"{pad_uid}_{local_idx}_0")
-        if "session_id" in sample:
-            sample["session_id"] = local_idx
-        pad_fields.append(sample)
+        pad_uid = -(local_idx + 1)
+        template_sample, template_tag = construct_minimal_padding_template(source_td, batch.tags[source_idx], eos_token_id, pad_uid)
+        pad_keys.append(f"{pad_uid}_0")
+        if "session_id" in template_sample:
+            template_sample["session_id"] = local_idx
+        pad_fields.append(template_sample)
         pad_tags.append(copy.deepcopy(template_tag))
 
     tq.kv_batch_put(
diff --git a/verl/trainer/ppo/rollout_corr_helper.py b/verl/trainer/ppo/rollout_corr_helper.py
index eed6de70..522c8077 100644
--- a/verl/trainer/ppo/rollout_corr_helper.py
+++ b/verl/trainer/ppo/rollout_corr_helper.py
@@ -762,23 +762,29 @@ def compute_is_metrics(
     if rollout_is_weights.dim() > 1:
         seq_mean_weights: torch.Tensor = verl_F.masked_mean(rollout_is_weights, response_mask, axis=-1)
 
-        metrics["rollout_is_seq_mean"] = seq_mean_weights.mean().item()
-        metrics["rollout_is_seq_std"] = seq_mean_weights.std().item() if seq_mean_weights.numel() > 1 else 0.0
-        metrics["rollout_is_seq_max"] = seq_mean_weights.max().item()
-        metrics["rollout_is_seq_min"] = seq_mean_weights.min().item()
-
-        # Sequence deviation from ideal weight (1.0)
-        seq_deviation: torch.Tensor = (seq_mean_weights - 1.0).abs()
-        metrics["rollout_is_seq_max_deviation"] = seq_deviation.max().item()
-
-        # Fraction of sequences with extreme weights. Use the raw (pre-truncation)
-        # per-sequence mean: every clamped weight is <= rollout_is_threshold, so a mean
-        # of clamped weights can never exceed it and the high fraction would be dead.
-        raw_seq_mean_weights: torch.Tensor = verl_F.masked_mean(raw_rollout_is_weights, response_mask, axis=-1)
-        metrics["rollout_is_seq_fraction_high"] = (raw_seq_mean_weights > rollout_is_threshold).float().mean().item()
-        metrics["rollout_is_seq_fraction_low"] = (
-            (raw_seq_mean_weights < rollout_is_threshold_lower).float().mean().item()
-        )
+        seq_valid_mask_is: torch.Tensor = response_mask.sum(dim=-1) > 0
+        seq_mean_weights_valid: torch.Tensor = seq_mean_weights[seq_valid_mask_is]
+
+        if seq_mean_weights_valid.numel() > 0:
+            metrics["rollout_is_seq_mean"] = seq_mean_weights_valid.mean().item()
+            metrics["rollout_is_seq_std"] = (
+                seq_mean_weights_valid.std().item() if seq_mean_weights_valid.numel() > 1 else 0.0
+            )
+            metrics["rollout_is_seq_max"] = seq_mean_weights_valid.max().item()
+            metrics["rollout_is_seq_min"] = seq_mean_weights_valid.min().item()
+
+            # Sequence deviation from ideal weight (1.0)
+            seq_deviation: torch.Tensor = (seq_mean_weights - 1.0).abs()
+            metrics["rollout_is_seq_max_deviation"] = seq_deviation.max().item()
+
+            # Fraction of sequences with extreme weights. Use the raw (pre-truncation)
+            # per-sequence mean: every clamped weight is <= rollout_is_threshold, so a mean
+            # of clamped weights can never exceed it and the high fraction would be dead.
+            raw_seq_mean_weights: torch.Tensor = verl_F.masked_mean(raw_rollout_is_weights, response_mask, axis=-1)
+            metrics["rollout_is_seq_fraction_high"] = (raw_seq_mean_weights > rollout_is_threshold).float().mean().item()
+            metrics["rollout_is_seq_fraction_low"] = (
+                (raw_seq_mean_weights < rollout_is_threshold_lower).float().mean().item()
+            )
 
     return metrics
 
diff --git a/verl/utils/checkpoint/megatron_checkpoint_manager.py b/verl/utils/checkpoint/megatron_checkpoint_manager.py
index 02be397d..1bdf7c54 100644
--- a/verl/utils/checkpoint/megatron_checkpoint_manager.py
+++ b/verl/utils/checkpoint/megatron_checkpoint_manager.py
@@ -175,6 +175,7 @@ class MegatronCheckpointManager(BaseCheckpointManager):
         bridge=None,
         provider=None,
         peft_cls=None,
+        use_per_rank_checkpoint: bool = False,
         **kwargs,
     ):
         super().__init__(
@@ -201,8 +202,12 @@ class MegatronCheckpointManager(BaseCheckpointManager):
         self.vanilla_bridge = self.provider is None
         self.peft_cls = peft_cls
         self.use_megatron_fsdp = use_megatron_fsdp
+        self.use_per_rank_checkpoint = use_per_rank_checkpoint and not self.use_megatron_fsdp
         self.rank = torch.distributed.get_rank()
 
+        if self.use_per_rank_checkpoint and self.checkpoint_config.async_save:
+            raise ValueError("Per-rank checkpoints do not support asynchronous saves.")
+
         # ``use_dist_checkpointing`` selects Megatron shards for the ``model``
         # slot; HF weights always go through ``bridge`` when requested by
         # ``save_contents`` / ``load_contents``.
@@ -495,6 +500,8 @@ class MegatronCheckpointManager(BaseCheckpointManager):
         transformer_config_rel = _rel(get_transformer_config_checkpoint_path(local_path))
 
         contents: dict[str, dict] = {}
+        regular_format = "megatron_per_rank" if self.use_per_rank_checkpoint else "megatron_dist_checkpoint"
+        regular_backend = "torch_save_per_rank" if self.use_per_rank_checkpoint else "dist_checkpointing"
 
         if self.should_save_hf_model:
             model_entry: dict = {
@@ -521,33 +528,33 @@ class MegatronCheckpointManager(BaseCheckpointManager):
         if self.should_save_dist_ckpt_model:
             contents["model"] = {
                 "path": model_dist_rel,
-                "format": "megatron_dist_checkpoint",
-                "backend": "dist_checkpointing",
+                "format": regular_format,
+                "backend": regular_backend,
             }
 
         if self.should_save_hf_model and self.peft_cls is not None:
             contents["peft_adapters"] = {
                 "path": model_dist_rel,
-                "format": "megatron_dist_checkpoint",
+                "format": regular_format,
                 "note": "PEFT adapter shards sit next to base-model HF weights, under model/dist_ckpt/.",
             }
 
         if self.should_save_optimizer:
             contents["optimizer"] = {
                 "path": optim_dist_rel,
-                "format": "megatron_dist_checkpoint",
+                "format": regular_format,
             }
             if self.lr_scheduler is not None:
                 contents["lr_scheduler"] = {
                     "path": optim_dist_rel,
-                    "format": "megatron_dist_checkpoint",
+                    "format": regular_format,
                     "key": "lr_scheduler",
                 }
 
         if self.should_save_extra:
             contents["rng_state"] = {
                 "path": extra_dist_rel,
-                "format": "megatron_dist_checkpoint",
+                "format": regular_format,
                 "key": "rng_state",
             }
             contents["transformer_config"] = {
@@ -840,11 +847,22 @@ class MegatronCheckpointManager(BaseCheckpointManager):
             raise KeyError
         tensor_parallel.get_cuda_rng_tracker().set_states(rng_states["rng_tracker_states"])
 
+    def _per_rank_checkpoint_metadata(self) -> dict:
+        """Return non-topology metadata validated by the per-rank checkpoint backend."""
+        return {
+            "optimizer_mode": "distributed" if self.use_distributed_optimizer else "replicated",
+            "architecture": self.arch,
+        }
+
     def _load_content_metadata(self, ckpt_dir: str) -> dict:
         """Fetch MCore's content_metadata for a dist_ckpt dir with a fallback."""
         load_content_metadata = getattr(dist_checkpointing, "load_content_metadata", None)
         metadata = None
-        if load_content_metadata is not None and self._has_checkpoint_files(ckpt_dir):
+        if (
+            not self.use_per_rank_checkpoint
+            and load_content_metadata is not None
+            and self._has_checkpoint_files(ckpt_dir)
+        ):
             metadata = load_content_metadata(checkpoint_dir=ckpt_dir)
         if metadata is None:
             if self.use_distributed_optimizer:
@@ -853,6 +871,22 @@ class MegatronCheckpointManager(BaseCheckpointManager):
                 metadata = self._build_sharded_state_dict_metadata()
         return metadata
 
+    def _load_dist_checkpoint(self, sharded_state_dict: dict, ckpt_dir: str) -> dict:
+        """Load one checkpoint content tree through the selected regular Megatron backend."""
+        if not self.use_per_rank_checkpoint:
+            return load_dist_checkpointing(
+                sharded_state_dict=sharded_state_dict,
+                ckpt_dir=ckpt_dir,
+            )
+
+        from psrl.utils.checkpoint import load_megatron_checkpoint
+
+        return load_megatron_checkpoint(
+            sharded_state_dict=sharded_state_dict,
+            ckpt_dir=ckpt_dir,
+            expected_metadata=self._per_rank_checkpoint_metadata(),
+        )
+
     def load_checkpoint(self, local_path: str, hdfs_path: str = None, del_local_after_load=False):
         """Load a Megatron checkpoint in the v2 split layout.
 
@@ -911,10 +945,13 @@ class MegatronCheckpointManager(BaseCheckpointManager):
         # ── Load model weights ──────────────────────────────────────────────
         if self.should_load_dist_ckpt_model or (self.should_load_hf_model and self.peft_cls is not None):
             model_sd = self._maybe_filter_peft_state_dict(dict(model_sharded_state_dict))
-            loaded_model = load_dist_checkpointing(
-                sharded_state_dict=model_sd,
-                ckpt_dir=model_dist_path,
+            log_with_rank(
+                f"Loading model checkpoint from {model_dist_path}",
+                rank=self.rank,
+                logger=logger,
+                log_only_rank_0=True,
             )
+            loaded_model = self._load_dist_checkpoint(model_sd, model_dist_path)
             assert "model" in loaded_model or any(
                 f"model{vpp_rank}" in loaded_model for vpp_rank in range(len(self.model))
             ), f"Model state dict not found in {loaded_model.keys()}. Please check the checkpoint file {local_path}."
@@ -939,10 +976,7 @@ class MegatronCheckpointManager(BaseCheckpointManager):
         # ── Load optimizer / LR scheduler ───────────────────────────────────
         if self.should_load_optimizer:
             optim_sd = self._build_optimizer_state_dict(model_sharded_state_dict, metadata, is_loading=True)
-            loaded_optim = load_dist_checkpointing(
-                sharded_state_dict=optim_sd,
-                ckpt_dir=optim_dist_path,
-            )
+            loaded_optim = self._load_dist_checkpoint(optim_sd, optim_dist_path)
             assert "optimizer" in loaded_optim, (
                 f"Optimizer state dict not found in {loaded_optim.keys()}. "
                 f"Please check the checkpoint file {optim_dist_path}."
@@ -963,10 +997,7 @@ class MegatronCheckpointManager(BaseCheckpointManager):
         # ── Load RNG states ─────────────────────────────────────────────────
         if self.should_load_extra:
             extra_sd = self._build_extra_state_dict()
-            loaded_extra = load_dist_checkpointing(
-                sharded_state_dict=extra_sd,
-                ckpt_dir=extra_dist_path,
-            )
+            loaded_extra = self._load_dist_checkpoint(extra_sd, extra_dist_path)
             assert "rng_state" in loaded_extra, (
                 f"RNG state dict not found in {loaded_extra.keys()}. "
                 f"Please check the checkpoint file {extra_dist_path}."
@@ -1052,29 +1083,50 @@ class MegatronCheckpointManager(BaseCheckpointManager):
             )
 
     def _save_dist_checkpoint(self, dist_checkpoint_path: str, state_dict: dict):
-        """Persist ``state_dict`` via Megatron dist_checkpointing.
+        """Persist ``state_dict`` through DCP or the synchronous per-rank backend.
 
-        Writes one self-contained ``torch_dist`` directory under
-        ``dist_checkpoint_path``.  Callers compose the exact state dict to go
-        into this directory; in the v2 layout there are up to three such
-        directories per checkpoint (``model/dist_ckpt/``,
-        ``optimizer/dist_ckpt/``, ``extra/dist_ckpt/``).
+        Callers compose the exact state dict written under ``dist_checkpoint_path``.
+        The v2 layout has up to three independent trees per checkpoint
+        (``model/dist_ckpt/``, ``optimizer/dist_ckpt/``, ``extra/dist_ckpt/``).
 
         Returns the async save request when ``async_save`` is enabled, or
         ``None`` for synchronous saves.
         """
         sharded_sd_metadata = self._build_sharded_state_dict_metadata()
 
-        async_save_request = save_dist_checkpointing(
-            sharded_state_dict=state_dict,
-            ckpt_path=dist_checkpoint_path,
-            async_save=self.checkpoint_config.async_save,
-            content_metadata=sharded_sd_metadata,
-        )
+        if self.use_per_rank_checkpoint:
+            from psrl.utils.checkpoint import save_megatron_checkpoint
+            log_with_rank(
+                f"Using PSRL per-rank checkpoint save (UCX-safe) to {dist_checkpoint_path}",
+                rank=self.rank,
+                logger=logger,
+                log_only_rank_0=True,
+            )
+            save_megatron_checkpoint(
+                sharded_state_dict=state_dict,
+                ckpt_path=dist_checkpoint_path,
+                async_save=False,
+                metadata=self._per_rank_checkpoint_metadata(),
+            )
+            async_save_request = None
+        else:
+            log_with_rank(
+                f"Using DCP save to {dist_checkpoint_path}",
+                rank=self.rank,
+                logger=logger,
+                log_only_rank_0=True,
+            )
+            async_save_request = save_dist_checkpointing(
+                sharded_state_dict=state_dict,
+                ckpt_path=dist_checkpoint_path,
+                async_save=self.checkpoint_config.async_save,
+                content_metadata=sharded_sd_metadata,
+            )
 
-        if not self.checkpoint_config.async_save:
-            assert async_save_request is None, "Async save request should be None when not using async save."
-            torch.distributed.barrier()
+            # Synchronize all async save requests
+            if not self.checkpoint_config.async_save:
+                assert async_save_request is None, "Async save request should be None when not using async save."
+                torch.distributed.barrier()
 
         return async_save_request
 
diff --git a/verl/utils/dataset/rl_dataset.py b/verl/utils/dataset/rl_dataset.py
index 5f47353e..d96bb2f1 100644
--- a/verl/utils/dataset/rl_dataset.py
+++ b/verl/utils/dataset/rl_dataset.py
@@ -27,7 +27,7 @@ from typing import Any, Optional
 import datasets
 import numpy as np
 import torch
-from omegaconf import DictConfig, ListConfig
+from omegaconf import DictConfig, ListConfig, OmegaConf
 from PIL import Image
 from torch.utils.data import Dataset
 from transformers import PreTrainedTokenizer, ProcessorMixin
@@ -120,6 +120,21 @@ class RLHFDataset(Dataset):
         self.apply_chat_template_kwargs = config.get("apply_chat_template_kwargs", {})
         self.mm_processor_kwargs = config.get("mm_processor_kwargs", {})
 
+        self.data_source_name = config.get("data_source_name", data_files[0])
+        default_reward_model_dict = {
+            "reward_loop_type": "naive",
+            "reward_fn": "default",
+            "reward_model_name": "null",
+            "reward_coef": 1.0,
+        }
+        if config.get("reward_model_dicts", None):
+            self.reward_model_dicts = OmegaConf.to_container(
+                config.get("reward_model_dicts", [default_reward_model_dict]),
+                resolve=True,
+            )
+        else:
+            self.reward_model_dicts = None
+
         # Mirror AgentLoopWorker's tool loading so length filtering sees the
         # same schemas the rollout will.
         self.tool_config_path = config.get("tool_config_path", None)
@@ -283,6 +298,17 @@ class RLHFDataset(Dataset):
         else:
             print(r"old dataloader ckpt file is used, please train from scratch for better ckpt performance")
 
+    def over_sample_dataset(self, over_sample_ratio: float = 1.0):
+        assert over_sample_ratio >= 1.0, "over_sample_ratio must be >= 1.0"
+
+        total = len(self.dataframe)
+        target_size = int(np.ceil(total * over_sample_ratio))
+        repeat_times = int(np.ceil(over_sample_ratio))
+        indices = np.tile(np.arange(total), repeat_times)[:target_size]
+
+        self.dataframe = self.dataframe.select(indices.tolist())
+        print(f"over sampled dataset len: {len(self.dataframe)}")
+
     def __getstate__(self):
         if not self.serialize_dataset:
             state = self.__dict__.copy()
@@ -407,6 +433,13 @@ class RLHFDataset(Dataset):
             logger.warning("tools_kwargs is empty for index %s, data source: %s", index, row_dict["data_source"])
         row_dict["index"] = index
         row_dict["tools_kwargs"] = tools_kwargs
+        # add reward loop type and reward fn
+        row_dict["reward_model_dicts"] = self.reward_model_dicts
+
+        data_source = row_dict.get("data_source", None)
+        if data_source is None:
+            data_source = self.data_source_name
+        row_dict["data_source"] = data_source
         row_dict["interaction_kwargs"] = interaction_kwargs
         return row_dict
 
diff --git a/verl/utils/experimental/torch_functional.py b/verl/utils/experimental/torch_functional.py
index d92ccb69..7da506c0 100644
--- a/verl/utils/experimental/torch_functional.py
+++ b/verl/utils/experimental/torch_functional.py
@@ -38,8 +38,17 @@ def _fused_linear_for_ppo_fwd(
     entropy = torch.logsumexp(logits, dim=-1) - torch.sum(probs * logits, dim=-1)
 
     if _FLASH_ATTN_CROSS_ENTROPY_AVAILABLE:
-        per_token_entropy_loss = cross_entropy_loss(logits, input_ids)[0]
-        token_log_probs = -per_token_entropy_loss
+        # flash-attn's Triton kernel derives `n_rows, n_cols = logits.shape` and asserts
+        # `labels.shape == (n_rows,)`, so it accepts only 2D logits with 1D labels while
+        # `hidden_states` here are batched. Flatten for the kernel and restore the caller's shape.
+        flat_logits = logits.reshape(-1, logits.shape[-1])
+        flat_labels = input_ids.reshape(-1)
+        assert flat_logits.shape[0] == flat_labels.shape[0], (
+            f"token count mismatch: {flat_logits.shape[0]} logits rows vs "
+            f"{flat_labels.shape[0]} labels; Ulysses SP slices labels but not VLM hidden states"
+        )
+        per_token_entropy_loss = cross_entropy_loss(flat_logits, flat_labels)[0]
+        token_log_probs = -per_token_entropy_loss.view(input_ids.shape)
     else:
         # Fallback to original PyTorch implementation
         log_probs = logits.log_softmax(dim=-1)
diff --git a/verl/utils/megatron/dist_checkpointing.py b/verl/utils/megatron/dist_checkpointing.py
index 98c0e546..5ca397c4 100644
--- a/verl/utils/megatron/dist_checkpointing.py
+++ b/verl/utils/megatron/dist_checkpointing.py
@@ -13,6 +13,7 @@
 # limitations under the License.
 
 import megatron.core
+import queue
 import torch
 from megatron.core import dist_checkpointing, mpu
 from megatron.core.dist_checkpointing.strategies.fully_parallel import (
@@ -25,6 +26,51 @@ from megatron.core.dist_checkpointing.strategies.torch import (
 )
 from packaging import version
 
+# ---------------------------------------------------------------------------
+# NCCL no-fork patch for DCP async save
+#
+# DCP's FileSystemWriterAsync.write_preloaded_data_multiproc forks child
+# processes via mp.get_context("fork"). Forked children inherit NCCL
+# communicators; when the child exits, ncclCommAbort runs and corrupts the
+# parent's NCCL state, eventually causing a 600-second timeout and SIGABRT.
+#
+# The fix: replace the multiproc entry-point with a sequential in-process
+# version that uses stdlib queue.SimpleQueue instead of forking.
+# ---------------------------------------------------------------------------
+from megatron.core.dist_checkpointing.strategies import filesystem_async as _fsa
+
+_orig_write_preloaded_data = _fsa.FileSystemWriterAsync.write_preloaded_data
+
+
+def _write_preloaded_data_multiproc_nofork(transform_list, use_msc, rank, write_buckets, global_results_queue):
+    """Sequential no-fork replacement for FileSystemWriterAsync.write_preloaded_data_multiproc.
+
+    Processes write buckets one at a time in the current process to avoid
+    forking NCCL communicators which causes ncclCommAbort corruption.
+    """
+    write_results = {}
+    for i, write_bucket in enumerate(write_buckets):
+        results_q = queue.SimpleQueue()
+        count_q = queue.Queue()
+        count_q.put(i)
+        kwargs = {
+            "local_proc_idx": i,
+            "write_bucket": write_bucket,
+            "results_queue": results_q,
+            "count_queue": count_q,
+            "use_fsync": True,
+        }
+        if use_msc:
+            kwargs["use_msc"] = use_msc
+        _orig_write_preloaded_data(transform_list, **kwargs)
+        local_proc_idx, local_results_or_exc = results_q.get()
+        if isinstance(local_results_or_exc, Exception):
+            raise local_results_or_exc
+        write_results[local_proc_idx] = local_results_or_exc
+    global_results_queue.put(write_results)
+
+
+_fsa.FileSystemWriterAsync.write_preloaded_data_multiproc = staticmethod(_write_preloaded_data_multiproc_nofork)
 
 def save_dist_checkpointing(
     sharded_state_dict,
diff --git a/verl/utils/tokenizer/tokenizer.py b/verl/utils/tokenizer/tokenizer.py
index 65f528bb..2ce27eb2 100644
--- a/verl/utils/tokenizer/tokenizer.py
+++ b/verl/utils/tokenizer/tokenizer.py
@@ -13,7 +13,7 @@
 # limitations under the License.
 """Utils for tokenization."""
 
-import types
+import functools
 import warnings
 
 __all__ = [
@@ -236,9 +236,9 @@ def hf_processor(name_or_path, **kwargs):
                 raise ValueError(f"Unsupported processor type: {processor.__class__.__name__}")
 
         if model_class is not None:
-            processor.get_rope_index = types.MethodType(model_class.get_rope_index, processor)
+            processor.get_rope_index = functools.partial(model_class.get_rope_index, processor)
             if hasattr(model_class, "get_vision_position_ids"):
-                processor.get_vision_position_ids = types.MethodType(model_class.get_vision_position_ids, processor)
+                processor.get_vision_position_ids = functools.partial(model_class.get_vision_position_ids, processor)
     except Exception as e:
         processor = None
         # TODO(haibin.lin): try-catch should be removed after adding transformer version req to setup.py to avoid
diff --git a/verl/workers/config/actor.py b/verl/workers/config/actor.py
index 33328a24..49a084bb 100644
--- a/verl/workers/config/actor.py
+++ b/verl/workers/config/actor.py
@@ -217,6 +217,7 @@ class ActorConfig(BaseConfig):
             "seq-mean-token-sum",
             "seq-mean-token-mean",
             "seq-mean-token-sum-norm",
+            "session-mean-token-mean",
         ]
         if self.loss_agg_mode not in valid_loss_agg_modes:
             raise ValueError(f"Invalid loss_agg_mode: {self.loss_agg_mode}")
diff --git a/verl/workers/config/engine.py b/verl/workers/config/engine.py
index 6854f493..b6e23d24 100644
--- a/verl/workers/config/engine.py
+++ b/verl/workers/config/engine.py
@@ -111,6 +111,7 @@ class EngineConfig(BaseConfig):
     use_remove_padding: bool = True
 
     seed: int = 42
+    load_weight: bool = True
 
     full_determinism: bool = False
     router_replay: EngineRouterReplayConfig = field(default_factory=EngineRouterReplayConfig)
@@ -200,6 +201,7 @@ class McoreEngineConfig(EngineConfig):
     pad_to_length: bool = False
     pad_to_length_bucket: int = 512
     use_dist_checkpointing: bool = False
+    use_per_rank_checkpoint: bool = False
     dist_checkpointing_path: Optional[str] = None
     dist_checkpointing_prefix: str = ""
     dist_ckpt_optim_fully_reshardable: bool = False
diff --git a/verl/workers/engine/fsdp/transformer_impl.py b/verl/workers/engine/fsdp/transformer_impl.py
index dd5cf331..e34d1962 100644
--- a/verl/workers/engine/fsdp/transformer_impl.py
+++ b/verl/workers/engine/fsdp/transformer_impl.py
@@ -30,6 +30,7 @@ from tensordict import TensorDict
 from torch.distributed.fsdp import FullyShardedDataParallel as FSDP
 from torch.distributed.fsdp.api import FullStateDictConfig, ShardedStateDictConfig, StateDictType
 from torch.distributed.tensor import DTensor
+from tqdm import tqdm
 
 import verl.utils.torch_functional as verl_F
 from verl.models.transformers.monkey_patch import apply_monkey_patch
@@ -78,6 +79,13 @@ from ..base import BaseEngine, BaseEngineCtx, EngineRegistry
 from ..utils import enable_full_determinism, pad_packed_inputs, postprocess_batch_func, prepare_micro_batches
 from .utils import create_device_mesh, get_sharding_strategy, unfuse_moe_params
 
+USE_TMS = False
+if os.environ.get("PSRL_TMS_ENABLE", "0") == "1":
+    USE_TMS = True
+    from torch_memory_saver import torch_memory_saver
+else:
+    torch_memory_saver = None
+
 logger = logging.getLogger(__file__)
 logger.setLevel(os.getenv("VERL_LOGGING_LEVEL", "WARN"))
 
@@ -254,12 +262,19 @@ class FSDPEngine(BaseEngine):
             if self.model_config.model_type == "language_model":
                 auto_class = get_hf_auto_model_class(hf_config=self.model_config.hf_config)
 
-                module = auto_class.from_pretrained(
-                    pretrained_model_name_or_path=self.model_config.local_path,
-                    torch_dtype=torch_dtype,
-                    config=self.model_config.hf_config,
-                    trust_remote_code=self.model_config.trust_remote_code,
-                )
+                if self.engine_config.load_weight:
+                    module = auto_class.from_pretrained(
+                        pretrained_model_name_or_path=self.model_config.local_path,
+                        torch_dtype=torch_dtype,
+                        config=self.model_config.hf_config,
+                        trust_remote_code=self.model_config.trust_remote_code,
+                    )
+                else:
+                    module = auto_class.from_config(
+                        self.model_config.hf_config,
+                        torch_dtype=torch_dtype,
+                        trust_remote_code=self.model_config.trust_remote_code,
+                    )
 
                 # Strip sub-modules listed in _verl_strip_modules (e.g.
                 # talker / code2wav for Qwen3-Omni Thinker-only training).
@@ -278,12 +293,20 @@ class FSDPEngine(BaseEngine):
                 self.model_config.hf_config.classifier_dropout = 0.0
                 self.model_config.hf_config.hidden_dropout = "0"
                 self.model_config.hf_config.summary_dropout_prob = 0.0
-                module = load_valuehead_model(
-                    local_path=self.model_config.local_path,
-                    torch_dtype=torch_dtype,
-                    model_config=self.model_config.hf_config,
-                    trust_remote_code=self.model_config.trust_remote_code,
-                )
+                if self.engine_config.load_weight:
+                    module = load_valuehead_model(
+                        local_path=self.model_config.local_path,
+                        torch_dtype=torch_dtype,
+                        model_config=self.model_config.hf_config,
+                        trust_remote_code=self.model_config.trust_remote_code,
+                    )
+                else:
+                    from transformers import AutoModelForTokenClassification
+
+                    module = AutoModelForTokenClassification.from_config(
+                        self.model_config.hf_config,
+                        torch_dtype=torch_dtype,
+                    )
 
             use_liger = self.model_config.use_liger
             # Apply Liger kernel; disable fused_linear_cross_entropy (conflicts with verl's forward patching)
@@ -569,36 +592,40 @@ class FSDPEngine(BaseEngine):
     def _build_model_optimizer(self):
         from verl.utils.model import print_model_size
 
-        # Load base model with specified configuration and dtype
-        module = self._build_module()
-        try:
-            self.pass_packed_cu_seqlens = "cu_seqlens" in signature(module.forward).parameters
-        except (TypeError, ValueError):
-            self.pass_packed_cu_seqlens = False
-        # Apply LoRA adapters if low-rank adaptation is enabled
-        if self._is_lora:
-            module = self._build_lora_module(module)
-
-        # Apply QAT before FSDP wrapping (training only)
-        if self._qat_enabled and not self.engine_config.forward_only:
-            module = self._apply_qat(module)
-
-        # Synchronize all distributed processes before proceeding
-        torch.distributed.barrier()
-        if self.rank == 0:
-            print_model_size(module)
-        log_gpu_memory_usage("After init model from HF AutoModel", logger=logger)
-
-        # Wrap model with FSDP for distributed training (sharding, mixed precision, etc.)
-        log_gpu_memory_usage("Before FSDP", logger=None)
-        module = self._build_fsdp_module(module)
-        log_gpu_memory_usage("After FSDP", logger=None)
+        tms_weight_ctx = torch_memory_saver.region() if USE_TMS else nullcontext()
+        with tms_weight_ctx:
+            # Load base model with specified configuration and dtype
+            module = self._build_module()
+            try:
+                self.pass_packed_cu_seqlens = "cu_seqlens" in signature(module.forward).parameters
+            except (TypeError, ValueError):
+                self.pass_packed_cu_seqlens = False
+            # Apply LoRA adapters if low-rank adaptation is enabled
+            if self._is_lora:
+                module = self._build_lora_module(module)
+
+            # Apply QAT before FSDP wrapping (training only)
+            if self._qat_enabled and not self.engine_config.forward_only:
+                module = self._apply_qat(module)
+
+            # Synchronize all distributed processes before proceeding
+            torch.distributed.barrier()
+            if self.rank == 0:
+                print_model_size(module)
+            log_gpu_memory_usage("After init model from HF AutoModel", logger=logger)
+
+            # Wrap model with FSDP for distributed training (sharding, mixed precision, etc.)
+            log_gpu_memory_usage("Before FSDP", logger=None)
+            module = self._build_fsdp_module(module)
+            log_gpu_memory_usage("After FSDP", logger=None)
 
         if not self.engine_config.forward_only:
-            # Initialize optimizer with model parameters and config settings
-            optimizer = self._build_optimizer(module)
-            # Create learning rate scheduler with warmup and decay settings
-            lr_scheduler = self._build_lr_scheduler(optimizer)
+            tms_optim_ctx = torch_memory_saver.region(enable_cpu_backup=True) if USE_TMS else nullcontext()
+            with tms_optim_ctx:
+                # Initialize optimizer with model parameters and config settings
+                optimizer = self._build_optimizer(module)
+                # Create learning rate scheduler with warmup and decay settings
+                lr_scheduler = self._build_lr_scheduler(optimizer)
         else:
             optimizer = None
             lr_scheduler = None
@@ -722,7 +749,21 @@ class FSDPEngine(BaseEngine):
         # and _build_fsdp_module, so self.scaler may not be set.
         scaler = getattr(self, "scaler", None)
 
-        for micro_batch_idx, micro_batch in enumerate(micro_batches):
+        # Micro-batch progress, rank 0 only so the training workers do not interleave. This loop is
+        # where a step spends most of its time, and without progress a slow step is
+        # indistinguishable from a hung one.
+        micro_batch_iter = enumerate(micro_batches)
+        if getattr(self, "rank", -1) == 0:
+            phase = "recompute log_prob" if forward_only else "actor training"
+            step = tu.get(data, "global_steps", None)
+            if isinstance(step, (list, tuple)):
+                step = step[0] if step else None
+            desc = f"step {step} | {phase}" if step is not None else phase
+            micro_batch_iter = enumerate(
+                tqdm(micro_batches, desc=desc, total=len(micro_batches), dynamic_ncols=True, leave=False)
+            )
+
+        for micro_batch_idx, micro_batch in micro_batch_iter:
             sync_ctx = (
                 nullcontext()
                 if forward_only
diff --git a/verl/workers/engine/megatron/transformer_impl.py b/verl/workers/engine/megatron/transformer_impl.py
index 5d47dd06..cca934f6 100644
--- a/verl/workers/engine/megatron/transformer_impl.py
+++ b/verl/workers/engine/megatron/transformer_impl.py
@@ -15,6 +15,7 @@
 import inspect
 import logging
 import os
+from contextlib import nullcontext
 from functools import partial
 from typing import Any, Callable, ContextManager, Iterator
 
@@ -25,6 +26,7 @@ from megatron.core.package_info import __version__
 from megatron.core.pipeline_parallel import get_forward_backward_func
 from omegaconf import OmegaConf
 from tensordict import TensorDict
+from tqdm import tqdm
 
 import verl.utils.torch_functional as verl_F
 from verl.models.mcore import get_mcore_weight_converter
@@ -83,6 +85,13 @@ from ..base import BaseEngine, BaseEngineCtx, EngineRegistry
 from ..utils import postprocess_batch_func, prepare_micro_batches
 from .utils import set_random_seed
 
+USE_TMS = False
+if os.environ.get("PSRL_TMS_ENABLE", "0") == "1":
+    USE_TMS = True
+    from torch_memory_saver import torch_memory_saver
+else:
+    torch_memory_saver = None
+
 logger = logging.getLogger(__file__)
 logger.setLevel(os.getenv("VERL_LOGGING_LEVEL", "WARN"))
 
@@ -487,20 +496,21 @@ class MegatronEngine(BaseEngine):
         self.tf_config = updated_tf_config
         print(f"module: {len(module)}")
 
-        if self.engine_config.use_dist_checkpointing:
-            load_mcore_dist_weights(
-                module, self.engine_config.dist_checkpointing_path, is_value_model=self.is_value_model
-            )
-        else:
-            if self.vanilla_bridge:
-                self.bridge.load_weights(module, self.model_config.local_path)
-            else:
-                allowed_mismatched_params = []
-                if self.is_value_model:
-                    allowed_mismatched_params = ["output_layer.weight"]
-                self.bridge.load_hf_weights(
-                    module, self.model_config.local_path, allowed_mismatched_params=allowed_mismatched_params
+        if self.engine_config.load_weight:
+            if self.engine_config.use_dist_checkpointing:
+                load_mcore_dist_weights(
+                    module, self.engine_config.dist_checkpointing_path, is_value_model=self.is_value_model
                 )
+            else:
+                if self.vanilla_bridge:
+                    self.bridge.load_weights(module, self.model_config.local_path)
+                else:
+                    allowed_mismatched_params = []
+                    if self.is_value_model:
+                        allowed_mismatched_params = ["output_layer.weight"]
+                    self.bridge.load_hf_weights(
+                        module, self.model_config.local_path, allowed_mismatched_params=allowed_mismatched_params
+                    )
 
         if torch.distributed.get_rank() == 0:
             print_model_size(module[0])
@@ -567,85 +577,90 @@ class MegatronEngine(BaseEngine):
     def initialize(self):
         self._hf_export_tasks = None
         self._build_tf_config()
-        _check_dcp_unsupported_features(self.engine_config, self.model_config, tf_config=self.tf_config)
+        tms_weight_ctx = torch_memory_saver.region() if USE_TMS else nullcontext()
+        with tms_weight_ctx:
+            _check_dcp_unsupported_features(self.engine_config, self.model_config, tf_config=self.tf_config)
 
-        self.module = self._build_megatron_module()
+            self.module = self._build_megatron_module()
 
-        if self._qat_enabled and not self.engine_config.forward_only:
-            from verl.utils.modelopt import apply_qat_to_modules
+            if self._qat_enabled and not self.engine_config.forward_only:
+                from verl.utils.modelopt import apply_qat_to_modules
 
-            self.module = apply_qat_to_modules(self.module, self._qat_config)
+                self.module = apply_qat_to_modules(self.module, self._qat_config)
 
-        self._maybe_enable_fused_kernels()
-
-        if self.model_config.mtp.enable:
-            patch_engine_mtp(self.module, self.model_config)
-        elif (
-            self.engine_config.forward_only
-            and self.engine_config.override_transformer_config.get("mtp_num_layers") == 0
-        ):
-            from verl.models.mcore.mtp_patch import patch_postprocess
+            self._maybe_enable_fused_kernels()
 
-            for model in self.module:
-                patch_postprocess(model)
+            if self.model_config.mtp.enable:
+                patch_engine_mtp(self.module, self.model_config)
+            elif (
+                self.engine_config.forward_only
+                and self.engine_config.override_transformer_config.get("mtp_num_layers") == 0
+            ):
+                from verl.models.mcore.mtp_patch import patch_postprocess
 
-        # For forward_only, we don't need optimizer, lr_scheduler, checkpoint_mananager
-        if self.engine_config.forward_only:
-            self.optimizer = None
-            self.lr_scheduler = None
-            self.to(device="cpu", model=self._is_offload_param, optimizer=False, grad=False)
-            log_gpu_memory_usage("After offload model during init (forward_only)", logger=logger)
-            return
+                for model in self.module:
+                    patch_postprocess(model)
 
-        self.optimizer = self._build_optimizer()
-        self.lr_scheduler = self._build_lr_scheduler()
+            # For forward_only, we don't need optimizer, lr_scheduler, checkpoint_mananager
+            if self.engine_config.forward_only:
+                self.optimizer = None
+                self.lr_scheduler = None
+                self.to(device="cpu", model=self._is_offload_param, optimizer=False, grad=False)
+                log_gpu_memory_usage("After offload model during init (forward_only)", logger=logger)
+                return
 
-        full_reshardable = self.engine_config.dist_ckpt_optim_fully_reshardable
-        mem_eff = self.engine_config.distrib_optim_fully_reshardable_mem_efficient
+        tms_optim_ctx = torch_memory_saver.region(enable_cpu_backup=True) if USE_TMS else nullcontext()
+        with tms_optim_ctx:
+            self.optimizer = self._build_optimizer()
+            self.lr_scheduler = self._build_lr_scheduler()
 
-        tmp_config = OmegaConf.create(
-            {
-                "model": {"path": self.model_config.local_path},
-                "megatron": {
-                    "dist_ckpt_optim_fully_reshardable": full_reshardable,
-                    "distrib_optim_fully_reshardable_mem_efficient": mem_eff,
-                },
-            }
-        )
+            full_reshardable = self.engine_config.dist_ckpt_optim_fully_reshardable
+            mem_eff = self.engine_config.distrib_optim_fully_reshardable_mem_efficient
 
-        role = "actor" if not self.is_value_model else "critic"
+            tmp_config = OmegaConf.create(
+                {
+                    "model": {"path": self.model_config.local_path},
+                    "megatron": {
+                        "dist_ckpt_optim_fully_reshardable": full_reshardable,
+                        "distrib_optim_fully_reshardable_mem_efficient": mem_eff,
+                    },
+                }
+            )
 
-        self.checkpoint_mananager = MegatronCheckpointManager(
-            config=tmp_config,
-            checkpoint_config=self.checkpoint_config,
-            model_config=self.model_config.hf_config,
-            transformer_config=self.tf_config,
-            role=role,
-            model=self.module,
-            arch=self.model_config.architectures[0],
-            hf_config=self.model_config.hf_config,
-            param_dtype=self.param_dtype,
-            share_embeddings_and_output_weights=self.share_embeddings_and_output_weights,
-            processing_class=self.model_config.get_processor(),
-            optimizer=self.optimizer,
-            optimizer_scheduler=self.lr_scheduler,
-            use_distributed_optimizer=self.engine_config.use_distributed_optimizer,
-            use_checkpoint_opt_param_scheduler=self.optimizer_config.use_checkpoint_opt_param_scheduler,
-            use_dist_checkpointing=self.engine_config.use_dist_checkpointing,
-            bridge=self.bridge,
-            provider=self.provider,
-            peft_cls=self.peft_cls,
-            use_megatron_fsdp=self.engine_config.use_megatron_fsdp,
-        )
+            role = "actor" if not self.is_value_model else "critic"
+
+            self.checkpoint_mananager = MegatronCheckpointManager(
+                config=tmp_config,
+                checkpoint_config=self.checkpoint_config,
+                model_config=self.model_config.hf_config,
+                transformer_config=self.tf_config,
+                role=role,
+                model=self.module,
+                arch=self.model_config.architectures[0],
+                hf_config=self.model_config.hf_config,
+                param_dtype=self.param_dtype,
+                share_embeddings_and_output_weights=self.share_embeddings_and_output_weights,
+                processing_class=self.model_config.get_processor(),
+                optimizer=self.optimizer,
+                optimizer_scheduler=self.lr_scheduler,
+                use_distributed_optimizer=self.engine_config.use_distributed_optimizer,
+                use_checkpoint_opt_param_scheduler=self.optimizer_config.use_checkpoint_opt_param_scheduler,
+                use_dist_checkpointing=self.engine_config.use_dist_checkpointing,
+                bridge=self.bridge,
+                provider=self.provider,
+                peft_cls=self.peft_cls,
+                use_megatron_fsdp=self.engine_config.use_megatron_fsdp,
+                use_per_rank_checkpoint=self.engine_config.use_per_rank_checkpoint,
+            )
 
-        self.to(
-            device="cpu",
-            model=self._is_offload_param,
-            optimizer=self._is_offload_optimizer,
-            grad=self._is_offload_param,
-        )
+            self.to(
+                device="cpu",
+                model=self._is_offload_param,
+                optimizer=self._is_offload_optimizer,
+                grad=self._is_offload_param,
+            )
 
-        log_gpu_memory_usage("After offload model/optimizer/grad during init", logger=logger)
+            log_gpu_memory_usage("After offload model/optimizer/grad during init", logger=logger)
 
     def train_mode(self, **kwargs):
         """
@@ -944,17 +959,43 @@ class MegatronEngine(BaseEngine):
         # batch should be a list of batches inside micro-batches
         batch_generator = make_batch_generator(micro_batches, vpp_size=len(self.module))
 
+        # Track one iterator on global rank 0. Under VPP every model chunk owns an iterator over the
+        # same logical micro-batches, so tracking all of them would multiply the displayed total by
+        # the number of virtual pipeline stages.
+        progress_bar = None
+        if torch.distributed.get_rank() == 0:
+            phase = "recompute log_prob" if forward_only else "actor training"
+            step = tu.get(data, "global_steps", None)
+            if isinstance(step, (list, tuple)):
+                step = step[0] if step else None
+            desc = f"step {step} | {phase}" if step is not None else phase
+            progress_bar = tqdm(total=n_micro_batch, desc=desc, dynamic_ncols=True, leave=False)
+
+            def track_micro_batches(batch_iter):
+                for micro_batch in batch_iter:
+                    progress_bar.update(1)
+                    yield micro_batch
+
+            if isinstance(batch_generator, list):
+                batch_generator[0] = track_micro_batches(batch_generator[0])
+            else:
+                batch_generator = track_micro_batches(batch_generator)
+
         # TODO: we may use the new schedule instead
         # for flash-attn: (seq_len, batch_size, hidden_size) = (mbs*seq_len, 1, hidden_size)
-        losses_reduced = forward_backward_func(
-            forward_step_func=forward_step,
-            data_iterator=batch_generator,
-            model=self.module,
-            num_microbatches=n_micro_batch,
-            seq_length=1,  # the communication shape is obtained via p2p comm
-            micro_batch_size=1,  # the communication shape is obtained via p2p comm
-            forward_only=forward_only,
-        )
+        try:
+            losses_reduced = forward_backward_func(
+                forward_step_func=forward_step,
+                data_iterator=batch_generator,
+                model=self.module,
+                num_microbatches=n_micro_batch,
+                seq_length=1,  # the communication shape is obtained via p2p comm
+                micro_batch_size=1,  # the communication shape is obtained via p2p comm
+                forward_only=forward_only,
+            )
+        finally:
+            if progress_bar is not None:
+                progress_bar.close()
 
         if self.model_config.mtp.enable and mpu.is_pipeline_last_stage(ignore_virtual=True):
             # All CP ranks must participate in the all_reduce inside get_megatron_mtp_loss,
diff --git a/verl/workers/engine_workers.py b/verl/workers/engine_workers.py
index 9bc01244..e2807268 100644
--- a/verl/workers/engine_workers.py
+++ b/verl/workers/engine_workers.py
@@ -85,8 +85,6 @@ class TrainingWorker(Worker, DistProfilerExtension):
 
         from verl.workers.engine import BaseEngine, EngineRegistry
 
-        initialize_global_process_group_ray(timeout_second=None)
-
         set_numa_affinity()
 
         self.config = config
@@ -256,6 +254,7 @@ class TrainingWorker(Worker, DistProfilerExtension):
         epochs = tu.pop(data, key="epochs", default=1)
         seed = tu.pop(data, key="seed", default=42)
         dataloader_kwargs = tu.pop(data, key="dataloader_kwargs", default={})
+        advance_lr_scheduler = tu.pop(data, key="advance_lr_scheduler", default=True)
 
         assert mini_batch_size is not None or num_mini_batch is not None
 
@@ -304,7 +303,7 @@ class TrainingWorker(Worker, DistProfilerExtension):
                 tu.assign_non_tensor(
                     mini_batch_td,
                     global_token_num=NonTensorData(global_token_num),
-                    update_lr_scheduler=batch_idx == total_num_iterations - 1,
+                    update_lr_scheduler=advance_lr_scheduler and batch_idx == total_num_iterations - 1,
                     disable_auto_offload=True,
                 )
                 actor_output = self.train_batch(mini_batch_td)
@@ -532,6 +531,7 @@ class ActorRolloutRefWorker(Worker, DistProfilerExtension):
     @register(dispatch_mode=Dispatch.ONE_TO_ALL)
     def init_model(self):
         model_config: HFModelConfig = omega_conf_to_dataclass(self.config.model)
+        initialize_global_process_group_ray(timeout_second=None)
 
         # 1. build reference model
         if "ref" in self.role:
diff --git a/verl/workers/utils/losses.py b/verl/workers/utils/losses.py
index ebaa4e4d..64fc8171 100644
--- a/verl/workers/utils/losses.py
+++ b/verl/workers/utils/losses.py
@@ -83,6 +83,24 @@ def ppo_loss(config: ActorConfig, model_output, data: TensorDict, dp_group=None)
     metrics = {}
 
     # select fields and convert to padded tensor
+    loss_mode = config.policy_loss.get("loss_mode", "vanilla")
+    # Weights remain dense row scalars while response fields are padded below.
+    config.global_batch_info.pop("session_loss_weights", None)
+    if config.loss_agg_mode == "session-mean-token-mean":
+        if loss_mode in {"gspo", "sapo", "geo_mean"}:
+            raise ValueError(
+                f"Policy loss {loss_mode!r} fixes its own aggregation and cannot use session-mean-token-mean."
+            )
+        correction = config.policy_loss.get("rollout_correction")
+        if loss_mode == "bypass_mode" and correction and correction.get("rollout_rs") is not None:
+            raise ValueError(
+                "session-mean-token-mean requires rejection masks before session normalization. "
+                "Use decoupled rollout correction instead of bypass rejection sampling."
+            )
+        if "session_loss_weights" not in data:
+            raise ValueError("session-mean-token-mean requires session_loss_weights in the training batch.")
+        config.global_batch_info["session_loss_weights"] = data["session_loss_weights"]
+        metric_aggregation = AggregationType.SUM
     fields = ["response_mask", "old_log_probs", "advantages"]
     if "rollout_is_weights" in data:
         fields.append("rollout_is_weights")
@@ -98,8 +116,6 @@ def ppo_loss(config: ActorConfig, model_output, data: TensorDict, dp_group=None)
 
     loss_agg_mode = config.loss_agg_mode
 
-    loss_mode = config.policy_loss.get("loss_mode", "vanilla")
-
     policy_loss_fn = get_policy_loss_fn(loss_mode)
     pg_loss, pg_metrics = policy_loss_fn(
         old_log_prob=old_log_prob,
