From 58d27a3ec7d94c104b66adea7acf6640d8385e70 Mon Sep 17 00:00:00 2001
From: Somoku <linsh0@protonmail.com>
Date: Thu, 4 Jun 2026 10:06:54 +0800
Subject: [PATCH] Basic patch for PSRL

- support specifying resource num per bundle to enable colocate of different resource pools
- support tms for weight and optim memory management
- patch DCP async save to avoid NCCL fork-corrupting timeout (no-fork replacement)
- add use_per_rank_checkpoint path in MegatronCheckpointManager
- support multi-dataset and multi-reward
---
 verl/protocol.py                              |  38 +++--
 verl/single_controller/ray/base.py            |   6 +-
 verl/trainer/config/data/legacy_data.yaml     |   2 +-
 verl/trainer/config/engine/fsdp.yaml          |   2 +
 verl/trainer/config/engine/megatron.yaml      |   2 +
 verl/trainer/ppo/metric_utils.py              |  38 ++++-
 verl/trainer/ppo/padding_utils.py             |  26 ++-
 verl/trainer/ppo/rollout_corr_helper.py       |  30 ++--
 .../checkpoint/megatron_checkpoint_manager.py |  62 +++++--
 verl/utils/dataset/rl_dataset.py              |  31 ++++
 verl/utils/megatron/dist_checkpointing.py     |  47 +++++
 verl/workers/config/engine.py                 |   1 +
 verl/workers/engine/fsdp/transformer_impl.py  |  59 ++++---
 .../engine/megatron/transformer_impl.py       | 160 ++++++++++--------
 14 files changed, 345 insertions(+), 159 deletions(-)

diff --git a/verl/protocol.py b/verl/protocol.py
index 59f91d83..2a67859f 100644
--- a/verl/protocol.py
+++ b/verl/protocol.py
@@ -936,22 +936,32 @@ class DataProto:
         # Merge meta_info with special handling for metrics
         merged_meta_info = {}
         if data:
-            # Merge non-metric meta_info and aggregate metrics from all workers.
-            all_metrics = []
+            # Collect all keys from all workers
+            all_keys = set()
             for d in data:
-                for k, v in d.meta_info.items():
-                    if k == "metrics":
+                all_keys.update(d.meta_info.keys())
+
+            # Merge non-metric meta_info and aggregate metrics from all workers.
+            all_metrics = []  # for key "metrics" only
+            for k in all_keys:
+                if k == "metrics":
+                    for d in data:
+                        v = d.meta_info.get(k)
                         if v is not None:
-                            if isinstance(v, list):
-                                all_metrics.extend(v)
-                            else:
-                                all_metrics.append(v)
-                    else:
-                        if k in merged_meta_info:
-                            # Ensure consistency for overlapping non-metric keys
-                            assert merged_meta_info[k] == v, f"Conflicting values for meta_info key '{k}'"
-                        else:
-                            merged_meta_info[k] = v
+                             if isinstance(v, list):
+                                 all_metrics.extend(v)
+                             else:
+                                 all_metrics.append(v)
+                elif k.endswith("_metrics"):
+                    # For xxx_metrics: merge as np.array([dict1, dict2, ...]) from each worker
+                    values = [d.meta_info.get(k) for d in data]
+                    merged_meta_info[k] = np.array(values)
+                else:
+                    # Ensure consistency for overlapping non-metric keys
+                    vals = [d.meta_info[k] for d in data if k in d.meta_info]
+                    if vals:
+                        assert all(vals[0] == v for v in vals), f"Conflicting values for meta_info key '{k}'"
+                        merged_meta_info[k] = vals[0]
 
             # Flatten list of dicts to dict of lists for consistent metrics structure
             if all_metrics:
diff --git a/verl/single_controller/ray/base.py b/verl/single_controller/ray/base.py
index 58b7148c..b04c64b8 100644
--- a/verl/single_controller/ray/base.py
+++ b/verl/single_controller/ray/base.py
@@ -118,6 +118,7 @@ class RayResourcePool(ResourcePool):
         max_colocate_count: int = 10,
         detached=False,
         accelerator_type: Optional[str] = None,
+        resource_num_per_bundle: Optional[float] = 1.0,
     ) -> None:
         super().__init__(process_on_nodes, max_colocate_count)
         self.use_gpu = use_gpu
@@ -126,6 +127,7 @@ class RayResourcePool(ResourcePool):
         self.pgs = None
         self.detached = detached
         self.accelerator_type = accelerator_type
+        self.resource_num_per_bundle = resource_num_per_bundle
 
     def get_placement_groups(self, strategy="STRICT_PACK", name=None, device_name="cuda"):
         if self.pgs is not None:
@@ -142,7 +144,7 @@ class RayResourcePool(ResourcePool):
 
         bundle = {"CPU": self.max_colocate_count}
         if self.use_gpu:
-            bundle[device_name] = 1
+            bundle[device_name] = self.resource_num_per_bundle
             if self.accelerator_type is not None:
                 bundle[self.accelerator_type] = 1e-4
         pg_scheme = [[bundle.copy() for _ in range(process_count)] for process_count in self._store]
@@ -624,7 +626,7 @@ class RayWorkerGroup(WorkerGroup):
         if self.use_gpu and not use_gpu:
             raise ValueError("use_gpu is True but resource_pool.use_gpu is False")
         local_world_size = resource_pool.store[0]
-        num_gpus = 1 / resource_pool.max_colocate_count
+        num_gpus = resource_pool.resource_num_per_bundle / resource_pool.max_colocate_count
 
         # we pass in environment variable at option so that Worker can use environment variable to set
         env_vars = {
diff --git a/verl/trainer/config/data/legacy_data.yaml b/verl/trainer/config/data/legacy_data.yaml
index cf4e7d5a..cda42ee5 100644
--- a/verl/trainer/config/data/legacy_data.yaml
+++ b/verl/trainer/config/data/legacy_data.yaml
@@ -62,7 +62,7 @@ return_raw_chat: True
 return_full_prompt: False
 
 # Whether to shuffle the data in the dataloader.
-shuffle: True
+shuffle: False
 
 # Seed to use when shuffling the data
 seed: null
diff --git a/verl/trainer/config/engine/fsdp.yaml b/verl/trainer/config/engine/fsdp.yaml
index ea690c02..3f88763f 100644
--- a/verl/trainer/config/engine/fsdp.yaml
+++ b/verl/trainer/config/engine/fsdp.yaml
@@ -38,6 +38,8 @@ use_orig_params: false
 # Random seed for reproducibility.
 seed: 42
 
+load_weight: true
+
 # Whether to enable full determinism for distributed training, only for debugging.
 full_determinism: false
 
diff --git a/verl/trainer/config/engine/megatron.yaml b/verl/trainer/config/engine/megatron.yaml
index 89d2dd2f..2dcc49b9 100644
--- a/verl/trainer/config/engine/megatron.yaml
+++ b/verl/trainer/config/engine/megatron.yaml
@@ -59,6 +59,8 @@ distrib_optim_fully_reshardable_mem_efficient: False
 # oc.select: default val for ref.megatron.seed
 seed: 42
 
+load_weight: true
+
 # Allow to override Distributed Data Parallel (DDP) config
 override_ddp_config: {}
 
diff --git a/verl/trainer/ppo/metric_utils.py b/verl/trainer/ppo/metric_utils.py
index 946f7611..ae2648b8 100644
--- a/verl/trainer/ppo/metric_utils.py
+++ b/verl/trainer/ppo/metric_utils.py
@@ -116,8 +116,8 @@ def compute_data_metrics(batch: DataProto, use_critic: bool = True) -> dict[str,
     advantages = batch.batch["advantages"]
     returns = batch.batch["returns"]
 
-    max_prompt_length = batch.batch["prompts"].shape[-1]
-    max_response_length = batch.batch["responses"].shape[-1]
+    max_prompt_length = batch.meta_info.get("max_prompt_length", batch.batch["prompts"].shape[-1])
+    max_response_length = batch.meta_info.get("max_response_length", batch.batch["responses"].shape[-1])
 
     response_mask = batch.batch["response_mask"].bool()
 
@@ -130,6 +130,26 @@ def compute_data_metrics(batch: DataProto, use_critic: bool = True) -> dict[str,
 
     non_aborted_sequence_score = sequence_score[non_aborted_mask]
     non_aborted_sequence_reward = sequence_reward[non_aborted_mask]
+    
+    if "original_reward_score" in batch.non_tensor_batch and "data_source" in batch.non_tensor_batch:
+        np_non_aborted_mask = non_aborted_mask.numpy()
+        non_aborted_original_scores = batch.non_tensor_batch["original_reward_score"][np_non_aborted_mask]
+        non_aborted_data_sources = batch.non_tensor_batch["data_source"][np_non_aborted_mask]
+        data_sources, data_source_counts = np.unique(non_aborted_data_sources, return_counts=True)
+        print("In this batch, the data sources are:")
+        for data_source, data_source_count in zip(data_sources, data_source_counts):
+            print(f"{data_source}: {data_source_count}")
+        print("--------------------------------")
+        data_src2original_score_metrics = defaultdict(dict)
+        for data_source in data_sources:
+            ds_mask = non_aborted_data_sources == data_source
+            ds_original_scores = non_aborted_original_scores[ds_mask]
+            data_src2original_score_metrics[data_source]["mean"] = np.mean(ds_original_scores)
+            data_src2original_score_metrics[data_source]["max"] = np.max(ds_original_scores)
+            data_src2original_score_metrics[data_source]["min"] = np.min(ds_original_scores)
+    else:
+        data_src2original_score_metrics = {}
+        data_sources = []
 
     if non_aborted_sequence_score.numel() > 0:
         score_mean = torch.mean(non_aborted_sequence_score).detach().item()
@@ -212,6 +232,15 @@ def compute_data_metrics(batch: DataProto, use_critic: bool = True) -> dict[str,
         critic_value_metrics = {}
 
     metrics = {
+        **{
+            key: value
+            for data_source in data_sources
+            for key, value in {
+                f"critic/original_score/{data_source}/mean": data_src2original_score_metrics[data_source]["mean"],
+                f"critic/original_score/{data_source}/max": data_src2original_score_metrics[data_source]["max"],
+                f"critic/original_score/{data_source}/min": data_src2original_score_metrics[data_source]["min"],
+            }.items()
+        },
         # score
         "critic/score/mean": score_mean,
         "critic/score/max": score_max,
@@ -636,8 +665,9 @@ def process_validation_metrics(
             var_dict = uid_dict.setdefault(uid, {})
 
             for var_name, var_vals in var2vals.items():
-                # skip empty or string values
-                if not var_vals or isinstance(var_vals[0], str):
+                # skip empty, string, or non-numeric values (e.g. dicts from
+                # reward_extra_info that aggregate per-loop reward metadata)
+                if not var_vals or not isinstance(var_vals[0], (int, float)):
                     continue
 
                 # compute mean and std
diff --git a/verl/trainer/ppo/padding_utils.py b/verl/trainer/ppo/padding_utils.py
index 436e17bc..011a0c76 100644
--- a/verl/trainer/ppo/padding_utils.py
+++ b/verl/trainer/ppo/padding_utils.py
@@ -71,6 +71,7 @@ def construct_minimal_padding_template(
     source_td: dict,
     source_tag: dict,
     eos_token_id: int,
+    pad_uid: int,
 ) -> tuple[dict, dict]:
     """Construct a minimal text-only padding template of one prompt token and one response token.
 
@@ -101,6 +102,7 @@ def construct_minimal_padding_template(
 
     # Update the fields and remove redundant parts
     template_sample.update(
+        uid=pad_uid,
         prompts=prompts,
         responses=prompts.clone(),
         input_ids=input_ids,
@@ -112,6 +114,8 @@ def construct_minimal_padding_template(
         rm_scores=torch.zeros_like(response_mask, dtype=torch.float32),
         rollout_log_probs=torch.zeros_like(response_mask, dtype=torch.float32),
     )
+    if "parent_id" in template_sample:
+        template_sample["parent_id"] = pad_uid
     if "multi_modal_inputs" in template_sample:
         template_sample["multi_modal_inputs"] = {}
     if routed_experts is not None:
@@ -120,7 +124,7 @@ def construct_minimal_padding_template(
         template_sample.pop("routed_experts", None)
 
     # Padding flag is deployed to protect metrics calculation (e.g. response length, score, reward).
-    template_tag.update(is_padding=True, prompt_len=1, response_len=1, seq_len=2)
+    template_tag.update(is_padding=True, prompt_len=1, response_len=1, seq_len=2, parent_id=pad_uid)
     return template_sample, template_tag
 
 
@@ -154,26 +158,18 @@ def upsample_batch_to_divisible_size(
     source_key = batch.keys[source_idx]
     source_td = tq.kv_batch_get(keys=[source_key], partition_id=batch.partition_id)[0]
 
-    # Construct the minimal padding template of one prompt token and one response token
-    template_sample, template_tag = construct_minimal_padding_template(source_td, batch.tags[source_idx], eos_token_id)
-
-    # All padding data use the same uid (also the same trajectory_id 0 but with ascending session_ids)
-    # This uid is not identical to any of the actual data, so it won't affect the grpo advantage value.
-    pad_uid = f"pad{uuid.uuid4().hex}"
-    template_sample["uid"] = pad_uid
-
     # Construct the padding samples in a for-loop
     pad_keys = []
     pad_tags = []
     pad_fields = []
     pad_size = batch_multiple - remainder
     for local_idx in range(pad_size):
-        sample = copy.deepcopy(template_sample)
-        # Use incremental local_idx as different session_ids
-        pad_keys.append(f"{pad_uid}_{local_idx}_0")
-        if "session_id" in sample:
-            sample["session_id"] = local_idx
-        pad_fields.append(sample)
+        pad_uid = -(local_idx + 1)
+        template_sample, template_tag = construct_minimal_padding_template(source_td, batch.tags[source_idx], eos_token_id, pad_uid)
+        pad_keys.append(f"{pad_uid}_0")
+        if "session_id" in template_sample:
+            template_sample["session_id"] = local_idx
+        pad_fields.append(template_sample)
         pad_tags.append(copy.deepcopy(template_tag))
 
     tq.kv_batch_put(
diff --git a/verl/trainer/ppo/rollout_corr_helper.py b/verl/trainer/ppo/rollout_corr_helper.py
index 0f4d558c..da740f11 100644
--- a/verl/trainer/ppo/rollout_corr_helper.py
+++ b/verl/trainer/ppo/rollout_corr_helper.py
@@ -760,18 +760,24 @@ def compute_is_metrics(
     if rollout_is_weights.dim() > 1:
         seq_mean_weights: torch.Tensor = verl_F.masked_mean(rollout_is_weights, response_mask, axis=-1)
 
-        metrics["rollout_is_seq_mean"] = seq_mean_weights.mean().item()
-        metrics["rollout_is_seq_std"] = seq_mean_weights.std().item() if seq_mean_weights.numel() > 1 else 0.0
-        metrics["rollout_is_seq_max"] = seq_mean_weights.max().item()
-        metrics["rollout_is_seq_min"] = seq_mean_weights.min().item()
-
-        # Sequence deviation from ideal weight (1.0)
-        seq_deviation: torch.Tensor = (seq_mean_weights - 1.0).abs()
-        metrics["rollout_is_seq_max_deviation"] = seq_deviation.max().item()
-
-        # Fraction of sequences with extreme weights
-        metrics["rollout_is_seq_fraction_high"] = (seq_mean_weights > rollout_is_threshold).float().mean().item()
-        metrics["rollout_is_seq_fraction_low"] = (seq_mean_weights < rollout_is_threshold_lower).float().mean().item()
+        seq_valid_mask_is: torch.Tensor = response_mask.sum(dim=-1) > 0
+        seq_mean_weights_valid: torch.Tensor = seq_mean_weights[seq_valid_mask_is]
+
+        if seq_mean_weights_valid.numel() > 0:
+            metrics["rollout_is_seq_mean"] = seq_mean_weights_valid.mean().item()
+            metrics["rollout_is_seq_std"] = (
+                seq_mean_weights_valid.std().item() if seq_mean_weights_valid.numel() > 1 else 0.0
+            )
+            metrics["rollout_is_seq_max"] = seq_mean_weights_valid.max().item()
+            metrics["rollout_is_seq_min"] = seq_mean_weights_valid.min().item()
+
+            # Sequence deviation from ideal weight (1.0)
+            seq_deviation: torch.Tensor = (seq_mean_weights - 1.0).abs()
+            metrics["rollout_is_seq_max_deviation"] = seq_deviation.max().item()
+
+            # Fraction of sequences with extreme weights
+            metrics["rollout_is_seq_fraction_high"] = (seq_mean_weights > rollout_is_threshold).float().mean().item()
+            metrics["rollout_is_seq_fraction_low"] = (seq_mean_weights < rollout_is_threshold_lower).float().mean().item()
 
     return metrics
 
diff --git a/verl/utils/checkpoint/megatron_checkpoint_manager.py b/verl/utils/checkpoint/megatron_checkpoint_manager.py
index 4416c6e4..d7d7cb19 100644
--- a/verl/utils/checkpoint/megatron_checkpoint_manager.py
+++ b/verl/utils/checkpoint/megatron_checkpoint_manager.py
@@ -175,6 +175,7 @@ class MegatronCheckpointManager(BaseCheckpointManager):
         bridge=None,
         provider=None,
         peft_cls=None,
+        use_per_rank_checkpoint: bool = False,
         **kwargs,
     ):
         super().__init__(
@@ -200,6 +201,7 @@ class MegatronCheckpointManager(BaseCheckpointManager):
         self.provider = provider
         self.vanilla_bridge = self.provider is None
         self.peft_cls = peft_cls
+        self.use_per_rank_checkpoint = use_per_rank_checkpoint
         self.use_megatron_fsdp = use_megatron_fsdp
         self.rank = torch.distributed.get_rank()
 
@@ -911,10 +913,25 @@ class MegatronCheckpointManager(BaseCheckpointManager):
         # ── Load model weights ──────────────────────────────────────────────
         if self.should_load_dist_ckpt_model or (self.should_load_hf_model and self.peft_cls is not None):
             model_sd = self._maybe_filter_peft_state_dict(dict(model_sharded_state_dict))
-            loaded_model = load_dist_checkpointing(
-                sharded_state_dict=model_sd,
-                ckpt_dir=model_dist_path,
-            )
+            if self.use_per_rank_checkpoint:
+                from psrl.utils.checkpoint import load_megatron_checkpoint
+                log_with_rank(
+                   f"Loading PSRL per-rank checkpoint from {model_dist_path}",
+                   rank=self.rank, logger=logger, log_only_rank_0=True
+                )
+                loaded_model = load_megatron_checkpoint(
+                    sharded_state_dict=model_sd,
+                    ckpt_dir=model_dist_path,
+                )
+            else:
+                log_with_rank(
+                    f"Loading DCP checkpoint from {model_dist_path}",
+                    rank=self.rank, logger=logger, log_only_rank_0=True
+                )
+                loaded_model = load_dist_checkpointing(
+                    sharded_state_dict=model_sd,
+                    ckpt_dir=model_dist_path,
+                )
             assert "model" in loaded_model or any(
                 f"model{vpp_rank}" in loaded_model for vpp_rank in range(len(self.model))
             ), f"Model state dict not found in {loaded_model.keys()}. Please check the checkpoint file {local_path}."
@@ -1065,16 +1082,35 @@ class MegatronCheckpointManager(BaseCheckpointManager):
         """
         sharded_sd_metadata = self._build_sharded_state_dict_metadata()
 
-        async_save_request = save_dist_checkpointing(
-            sharded_state_dict=state_dict,
-            ckpt_path=dist_checkpoint_path,
-            async_save=self.checkpoint_config.async_save,
-            content_metadata=sharded_sd_metadata,
-        )
-
-        if not self.checkpoint_config.async_save:
-            assert async_save_request is None, "Async save request should be None when not using async save."
+        if self.use_per_rank_checkpoint:
+            from psrl.utils.checkpoint import save_megatron_checkpoint
+            log_with_rank(
+                f"Using PSRL per-rank checkpoint save (UCX-safe) to {dist_checkpoint_path}",
+                rank=self.rank, logger=logger, log_only_rank_0=True
+            )
+            save_megatron_checkpoint(
+                sharded_state_dict=state_dict,
+                ckpt_path=dist_checkpoint_path,
+                async_save=False,  # PSRL doesn't support async
+            )
+            async_save_request = None
             torch.distributed.barrier()
+        else:
+            log_with_rank(
+                f"Using DCP save to {dist_checkpoint_path}",
+                rank=self.rank, logger=logger, log_only_rank_0=True
+            )
+            async_save_request = save_dist_checkpointing(
+                sharded_state_dict=state_dict,
+                ckpt_path=dist_checkpoint_path,
+                async_save=self.checkpoint_config.async_save,
+                content_metadata=sharded_sd_metadata,
+            )
+
+            # Synchronize all async save requests
+            if not self.checkpoint_config.async_save:
+                assert async_save_request is None, "Async save request should be None when not using async save."
+                torch.distributed.barrier()
 
         return async_save_request
 
diff --git a/verl/utils/dataset/rl_dataset.py b/verl/utils/dataset/rl_dataset.py
index dc4fcf07..a6a80fae 100644
--- a/verl/utils/dataset/rl_dataset.py
+++ b/verl/utils/dataset/rl_dataset.py
@@ -22,6 +22,7 @@ import traceback
 from collections import defaultdict
 from io import BytesIO
 from typing import Any, Optional
+from omegaconf import OmegaConf
 
 import datasets
 import numpy as np
@@ -117,6 +118,18 @@ class RLHFDataset(Dataset):
         self.apply_chat_template_kwargs = config.get("apply_chat_template_kwargs", {})
         self.mm_processor_kwargs = config.get("mm_processor_kwargs", {})
 
+        self.data_source_name = config.get("data_source_name", data_files[0])
+        default_reward_model_dict = {
+            "reward_loop_type": "naive",
+            "reward_fn": "default",
+            "reward_model_name": "null",
+            "reward_coef": 1.0,
+        }
+        self.reward_model_dicts = OmegaConf.to_container(
+            config.get("reward_model_dicts", [default_reward_model_dict]),
+            resolve=True,
+        )
+
         # Mirror AgentLoopWorker's tool loading so length filtering sees the
         # same schemas the rollout will.
         self.tool_config_path = config.get("tool_config_path", None)
@@ -280,6 +293,17 @@ class RLHFDataset(Dataset):
         else:
             print(r"old dataloader ckpt file is used, please train from scratch for better ckpt performance")
 
+    def over_sample_dataset(self, over_sample_ratio: float = 1.0):
+        assert over_sample_ratio >= 1.0, "over_sample_ratio must be >= 1.0"
+
+        total = len(self.dataframe)
+        target_size = int(np.ceil(total * over_sample_ratio))
+        repeat_times = int(np.ceil(over_sample_ratio))
+        indices = np.tile(np.arange(total), repeat_times)[:target_size]
+
+        self.dataframe = self.dataframe.select(indices.tolist())
+        print(f"over sampled dataset len: {len(self.dataframe)}")
+
     def __getstate__(self):
         if not self.serialize_dataset:
             state = self.__dict__.copy()
@@ -389,6 +413,13 @@ class RLHFDataset(Dataset):
             logger.warning("tools_kwargs is empty for index %s, data source: %s", index, row_dict["data_source"])
         row_dict["index"] = index
         row_dict["tools_kwargs"] = tools_kwargs
+        # add reward loop type and reward fn
+        row_dict["reward_model_dicts"] = self.reward_model_dicts
+
+        data_source = row_dict.get("data_source", None)
+        if data_source is None:
+            data_source = self.data_source_name
+        row_dict["data_source"] = data_source
         row_dict["interaction_kwargs"] = interaction_kwargs
         return row_dict
 
diff --git a/verl/utils/megatron/dist_checkpointing.py b/verl/utils/megatron/dist_checkpointing.py
index 08139661..e6b04036 100644
--- a/verl/utils/megatron/dist_checkpointing.py
+++ b/verl/utils/megatron/dist_checkpointing.py
@@ -13,6 +13,7 @@
 # limitations under the License.
 
 import megatron.core
+import queue
 import torch
 from megatron.core import dist_checkpointing, mpu
 from megatron.core.dist_checkpointing.serialization import (
@@ -25,6 +26,52 @@ from megatron.core.dist_checkpointing.strategies.fully_parallel import (
 )
 from packaging import version
 
+# ---------------------------------------------------------------------------
+# NCCL no-fork patch for DCP async save
+#
+# DCP's FileSystemWriterAsync.write_preloaded_data_multiproc forks child
+# processes via mp.get_context("fork"). Forked children inherit NCCL
+# communicators; when the child exits, ncclCommAbort runs and corrupts the
+# parent's NCCL state, eventually causing a 600-second timeout and SIGABRT.
+#
+# The fix: replace the multiproc entry-point with a sequential in-process
+# version that uses stdlib queue.SimpleQueue instead of forking.
+# ---------------------------------------------------------------------------
+from megatron.core.dist_checkpointing.strategies import filesystem_async as _fsa
+
+_orig_write_preloaded_data = _fsa.FileSystemWriterAsync.write_preloaded_data
+
+
+def _write_preloaded_data_multiproc_nofork(transform_list, use_msc, rank, write_buckets, global_results_queue):
+    """Sequential no-fork replacement for FileSystemWriterAsync.write_preloaded_data_multiproc.
+
+    Processes write buckets one at a time in the current process to avoid
+    forking NCCL communicators which causes ncclCommAbort corruption.
+    """
+    write_results = {}
+    for i, write_bucket in enumerate(write_buckets):
+        results_q = queue.SimpleQueue()
+        count_q = queue.Queue()
+        count_q.put(i)
+        kwargs = {
+            "local_proc_idx": i,
+            "write_bucket": write_bucket,
+            "results_queue": results_q,
+            "count_queue": count_q,
+            "use_fsync": True,
+        }
+        if use_msc:
+            kwargs["use_msc"] = use_msc
+        _orig_write_preloaded_data(transform_list, **kwargs)
+        local_proc_idx, local_results_or_exc = results_q.get()
+        if isinstance(local_results_or_exc, Exception):
+            raise local_results_or_exc
+        write_results[local_proc_idx] = local_results_or_exc
+    global_results_queue.put(write_results)
+
+
+_fsa.FileSystemWriterAsync.write_preloaded_data_multiproc = staticmethod(_write_preloaded_data_multiproc_nofork)
+
 
 def save_dist_checkpointing(
     sharded_state_dict,
diff --git a/verl/workers/config/engine.py b/verl/workers/config/engine.py
index b5ad8e9c..b6727a82 100644
--- a/verl/workers/config/engine.py
+++ b/verl/workers/config/engine.py
@@ -112,6 +112,7 @@ class EngineConfig(BaseConfig):
     use_remove_padding: bool = True
 
     seed: int = 42
+    load_weight: bool = True
 
     full_determinism: bool = False
     router_replay: EngineRouterReplayConfig = field(default_factory=EngineRouterReplayConfig)
diff --git a/verl/workers/engine/fsdp/transformer_impl.py b/verl/workers/engine/fsdp/transformer_impl.py
index 2c66c6a3..01ea9cbc 100644
--- a/verl/workers/engine/fsdp/transformer_impl.py
+++ b/verl/workers/engine/fsdp/transformer_impl.py
@@ -76,6 +76,13 @@ from ..base import BaseEngine, BaseEngineCtx, EngineRegistry
 from ..utils import enable_full_determinism, postprocess_batch_func, prepare_micro_batches
 from .utils import create_device_mesh, get_sharding_strategy
 
+USE_TMS = False
+if os.environ.get("PSRL_TMS_ENABLE", "0") == "1":
+    USE_TMS = True
+    from torch_memory_saver import torch_memory_saver
+else:
+    torch_memory_saver = None
+
 logger = logging.getLogger(__file__)
 logger.setLevel(os.getenv("VERL_LOGGING_LEVEL", "WARN"))
 
@@ -543,32 +550,36 @@ class FSDPEngine(BaseEngine):
     def _build_model_optimizer(self):
         from verl.utils.model import print_model_size
 
-        # Load base model with specified configuration and dtype
-        module = self._build_module()
-        # Apply LoRA adapters if low-rank adaptation is enabled
-        if self._is_lora:
-            module = self._build_lora_module(module)
-
-        # Apply QAT before FSDP wrapping (training only)
-        if self._qat_enabled and not self.engine_config.forward_only:
-            module = self._apply_qat(module)
-
-        # Synchronize all distributed processes before proceeding
-        torch.distributed.barrier()
-        if self.rank == 0:
-            print_model_size(module)
-        log_gpu_memory_usage("After init model from HF AutoModel", logger=logger)
-
-        # Wrap model with FSDP for distributed training (sharding, mixed precision, etc.)
-        log_gpu_memory_usage("Before FSDP", logger=None)
-        module = self._build_fsdp_module(module)
-        log_gpu_memory_usage("After FSDP", logger=None)
+        tms_weight_ctx = torch_memory_saver.region() if USE_TMS else nullcontext()
+        with tms_weight_ctx:
+            # Load base model with specified configuration and dtype
+            module = self._build_module()
+            # Apply LoRA adapters if low-rank adaptation is enabled
+            if self._is_lora:
+                module = self._build_lora_module(module)
+
+            # Apply QAT before FSDP wrapping (training only)
+            if self._qat_enabled and not self.engine_config.forward_only:
+                module = self._apply_qat(module)
+
+            # Synchronize all distributed processes before proceeding
+            torch.distributed.barrier()
+            if self.rank == 0:
+                print_model_size(module)
+            log_gpu_memory_usage("After init model from HF AutoModel", logger=logger)
+
+            # Wrap model with FSDP for distributed training (sharding, mixed precision, etc.)
+            log_gpu_memory_usage("Before FSDP", logger=None)
+            module = self._build_fsdp_module(module)
+            log_gpu_memory_usage("After FSDP", logger=None)
 
         if not self.engine_config.forward_only:
-            # Initialize optimizer with model parameters and config settings
-            optimizer = self._build_optimizer(module)
-            # Create learning rate scheduler with warmup and decay settings
-            lr_scheduler = self._build_lr_scheduler(optimizer)
+            tms_optim_ctx = torch_memory_saver.region(enable_cpu_backup=True) if USE_TMS else nullcontext()
+            with tms_optim_ctx:
+                # Initialize optimizer with model parameters and config settings
+                optimizer = self._build_optimizer(module)
+                # Create learning rate scheduler with warmup and decay settings
+                lr_scheduler = self._build_lr_scheduler(optimizer)
         else:
             optimizer = None
             lr_scheduler = None
diff --git a/verl/workers/engine/megatron/transformer_impl.py b/verl/workers/engine/megatron/transformer_impl.py
index 845d5b7c..4d349b71 100644
--- a/verl/workers/engine/megatron/transformer_impl.py
+++ b/verl/workers/engine/megatron/transformer_impl.py
@@ -69,6 +69,13 @@ from ..base import BaseEngine, BaseEngineCtx, EngineRegistry
 from ..utils import postprocess_batch_func, prepare_micro_batches
 from .utils import set_random_seed
 
+USE_TMS = False
+if os.environ.get("PSRL_TMS_ENABLE", "0") == "1":
+    USE_TMS = True
+    from torch_memory_saver import torch_memory_saver
+else:
+    torch_memory_saver = None
+
 logger = logging.getLogger(__file__)
 logger.setLevel(os.getenv("VERL_LOGGING_LEVEL", "WARN"))
 
@@ -367,83 +374,88 @@ class MegatronEngine(BaseEngine):
     def initialize(self):
         self._build_tf_config()
 
-        self.module = self._build_megatron_module()
-
-        if self._qat_enabled and not self.engine_config.forward_only:
-            from verl.utils.modelopt import apply_qat_to_modules
-
-            self.module = apply_qat_to_modules(self.module, self._qat_config)
-
-        self._maybe_enable_fused_kernels()
-
-        if self.model_config.mtp.enable:
-            patch_engine_mtp(self.module, self.model_config)
-        elif (
-            self.engine_config.forward_only
-            and self.engine_config.override_transformer_config.get("mtp_num_layers") == 0
-        ):
-            from verl.models.mcore.mtp_patch import patch_postprocess
-
-            for model in self.module:
-                patch_postprocess(model)
-
-        # For forward_only, we don't need optimizer, lr_scheduler, checkpoint_mananager
-        if self.engine_config.forward_only:
-            self.optimizer = None
-            self.lr_scheduler = None
-            self.to(device="cpu", model=self._is_offload_param, optimizer=False, grad=False)
-            log_gpu_memory_usage("After offload model during init (forward_only)", logger=logger)
-            return
-
-        self.optimizer = self._build_optimizer()
-        self.lr_scheduler = self._build_lr_scheduler()
-
-        full_reshardable = self.engine_config.dist_ckpt_optim_fully_reshardable
-        mem_eff = self.engine_config.distrib_optim_fully_reshardable_mem_efficient
-
-        tmp_config = OmegaConf.create(
-            {
-                "model": {"path": self.model_config.local_path},
-                "megatron": {
-                    "dist_ckpt_optim_fully_reshardable": full_reshardable,
-                    "distrib_optim_fully_reshardable_mem_efficient": mem_eff,
-                },
-            }
-        )
-
-        role = "actor" if not self.is_value_model else "critic"
+        tms_weight_ctx = torch_memory_saver.region() if USE_TMS else nullcontext()
+        with tms_weight_ctx:
+            self.module = self._build_megatron_module()
+
+            if self._qat_enabled and not self.engine_config.forward_only:
+                from verl.utils.modelopt import apply_qat_to_modules
+
+                self.module = apply_qat_to_modules(self.module, self._qat_config)
+
+            self._maybe_enable_fused_kernels()
+
+            if self.model_config.mtp.enable:
+                patch_engine_mtp(self.module, self.model_config)
+            elif (
+                self.engine_config.forward_only
+                and self.engine_config.override_transformer_config.get("mtp_num_layers") == 0
+            ):
+                from verl.models.mcore.mtp_patch import patch_postprocess
+
+                for model in self.module:
+                    patch_postprocess(model)
+
+            # For forward_only, we don't need optimizer, lr_scheduler, checkpoint_mananager
+            if self.engine_config.forward_only:
+                self.optimizer = None
+                self.lr_scheduler = None
+                self.to(device="cpu", model=self._is_offload_param, optimizer=False, grad=False)
+                log_gpu_memory_usage("After offload model during init (forward_only)", logger=logger)
+                return
+
+        tms_optim_ctx = torch_memory_saver.region(enable_cpu_backup=True) if USE_TMS else nullcontext()
+        with tms_optim_ctx:
+            self.optimizer = self._build_optimizer()
+            self.lr_scheduler = self._build_lr_scheduler()
+
+            full_reshardable = self.engine_config.dist_ckpt_optim_fully_reshardable
+            mem_eff = self.engine_config.distrib_optim_fully_reshardable_mem_efficient
+
+            tmp_config = OmegaConf.create(
+                {
+                    "model": {"path": self.model_config.local_path},
+                    "megatron": {
+                        "dist_ckpt_optim_fully_reshardable": full_reshardable,
+                        "distrib_optim_fully_reshardable_mem_efficient": mem_eff,
+                    },
+                }
+            )
 
-        self.checkpoint_mananager = MegatronCheckpointManager(
-            config=tmp_config,
-            checkpoint_config=self.checkpoint_config,
-            model_config=self.model_config.hf_config,
-            transformer_config=self.tf_config,
-            role=role,
-            model=self.module,
-            arch=self.model_config.architectures[0],
-            hf_config=self.model_config.hf_config,
-            param_dtype=self.param_dtype,
-            share_embeddings_and_output_weights=self.model_config.share_embeddings_and_output_weights,
-            processing_class=self.model_config.get_processor(),
-            optimizer=self.optimizer,
-            optimizer_scheduler=self.lr_scheduler,
-            use_distributed_optimizer=self.engine_config.use_distributed_optimizer,
-            use_checkpoint_opt_param_scheduler=self.optimizer_config.use_checkpoint_opt_param_scheduler,
-            use_dist_checkpointing=self.engine_config.use_dist_checkpointing,
-            bridge=self.bridge,
-            provider=self.provider,
-            peft_cls=self.peft_cls,
-            use_megatron_fsdp=self.engine_config.use_megatron_fsdp,
-        )
+            role = "actor" if not self.is_value_model else "critic"
+
+            self.checkpoint_mananager = MegatronCheckpointManager(
+                config=tmp_config,
+                checkpoint_config=self.checkpoint_config,
+                model_config=self.model_config.hf_config,
+                transformer_config=self.tf_config,
+                role=role,
+                model=self.module,
+                arch=self.model_config.architectures[0],
+                hf_config=self.model_config.hf_config,
+                param_dtype=self.param_dtype,
+                share_embeddings_and_output_weights=self.model_config.share_embeddings_and_output_weights,
+                processing_class=self.model_config.get_processor(),
+                optimizer=self.optimizer,
+                optimizer_scheduler=self.lr_scheduler,
+                use_distributed_optimizer=self.engine_config.use_distributed_optimizer,
+                use_checkpoint_opt_param_scheduler=self.optimizer_config.use_checkpoint_opt_param_scheduler,
+                use_dist_checkpointing=self.engine_config.use_dist_checkpointing,
+                use_per_rank_checkpoint=not self.psrl_config.checkpoint.use_dcp_save,
+                bridge=self.bridge,
+                provider=self.provider,
+                peft_cls=self.peft_cls,
+                use_megatron_fsdp=self.engine_config.use_megatron_fsdp,
+            )
 
-        self.to(
-            device="cpu",
-            model=self._is_offload_param,
-            optimizer=self._is_offload_optimizer,
-            grad=self._is_offload_param,
-        )
+            self.to(
+                device="cpu",
+                model=self._is_offload_param,
+                optimizer=self._is_offload_optimizer,
+                grad=self._is_offload_param,
+            )
 
-        log_gpu_memory_usage("After offload model/optimizer/grad during init", logger=logger)
+            log_gpu_memory_usage("After offload model/optimizer/grad during init", logger=logger)
 
     def train_mode(self, **kwargs):
         """
-- 
2.43.0

